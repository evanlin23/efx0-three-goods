# Failed attempts
One file per approach that did not work, ending with the smallest configuration where it breaks and a script that reproduces it. A failed attempt with a precise failure point is a result: it belongs in the final report.
