# Ledger: EFX₀ with at most three relevant goods per agent

The single source of truth. A status changes only in a pull request that adds the artifact in the Artifact column; CI checks that every PROVED, CERTIFIED or REFUTED row points to files that exist. The Lean column names machine-checked statements in `lean/` (core Lean, no `sorry`, standard axioms only; see `lean/README.md`); CI checks that each has a `#print axioms` certificate and that `lean/check.sh` passes.

| ID | Claim | Status | Artifact | Lean | Notes |
|---|---|---|---|---|---|
| L1 | For fixed (n, m): EFX₀ for all additive instances ⟺ EFX for all positive additive instances | PROVED | `proofs/lemmas.md` | | Step 0: complete proof. Transfers only results stated for classes of (n, m), not results about few relevant goods or (multi)graphs. No longer used by R1 |
| L2 | Peeling rules R1 and R2 | PROVED | `proofs/lemmas.md` | `EFX.peel`, `EFX.peelEmpty`, `EFX.peelR2`, `EFX.peelBundle` | Lean (over lists): R1, R1 with P = ∅, R2 (without its unused hypothesis that P has ≥ 2 goods), and the general peeling step that R2 and R1 with P = ∅ instantiate. `src/lemmas.py`: 1,500 random instances through the pipeline, 0 failures. Step 0: complete proof of the general peeling step (P worth ≥ i's remaining goods; |P| ≤ 1 or P worthless to the others); `src/step0_checks.py`: exhaustive on two small grids, 0 failures |
| L2c | ≤ 2 relevant goods per agent ⟹ EFX₀ (serial dictatorship; the HW1 theorem) | PROVED | `proofs/lemmas.md` | `EFX.exists_efx0_of_count` | stronger form (all bundles but one have ≤ 1 good) machine-checked in evanlin23/mrd-efx: `MRD.main_theorem_L`. Step 0: complete proof, and of the brief's remark on when serial dictatorship breaks at three goods |
| L3 | Worthless goods go to an envy-graph source | PROVED | `proofs/lemmas.md` | `EFX.junk`, `EFX.Inst.exists_efx0_of_junk`, `EFX.rotate_efx0`, `EFX.exists_unenvied` | same pipeline test. Lean: over lists and in the model; rotation preserves EFX₀, and rotating envy cycles reaches an allocation with an unenvied agent. Step 0: complete proof; `src/step0_checks.py`: exhaustive on two small grids, 0 failures |
| L4 | Core counting: 3n = 2m − π + Σ(deg − 2), so m ≤ 2n | PROVED | `proofs/lemmas.md` | | Step 0: complete proof; identity checked on 7,118 enumerated cores (`src/step0_checks.py`) |
| L5 | In a core, EFX₀ is ordinal and equals cases T/P/B/C/E | PROVED | `proofs/lemmas.md` | | `src/coreG.py`: 896,400 allocations vs. the definition, 0 mismatches. Step 0: complete proof; with ties the cases stay sufficient, so tied cores reduce to strict profiles. `src/step0_checks.py`: all 47 local configurations × 6 rankings × 95 realizations vs. the raw definition, 0 mismatches |
| L6 | Disconnected cores are solved component by component | PROVED | `proofs/lemmas.md` | | Step 0: complete proof. It composes EFX₀ allocations, but not conjecture D's shape (see D) |
| L7 | β = 2n − m + 1; alone-goods identity | PROVED | `proofs/lemmas.md` | | Step 0: complete proof; β checked on 7,118 cores, the alone-goods identity on all 5,670 size-≤ 2 partitions of the X2 goods |
| L8 | Two own goods ⟹ safe; β = 1 cores solved by orientation | PROVED | `proofs/lemmas.md` | `EFX.Inst.safe_of_two_own`, `EFX.envyFree_of_two_own`, `EFX.Inst.efx0_of_two_own`, `EFX.balance_needed` | Lean: the first half, for agents with a ≤ b + c (all core agents), and "every agent holds two own goods ⟹ EFX₀". Balance is necessary (`EFX.balance_needed`: a top-heavy counterexample). Not formalized: that a β = 1 core admits such an allocation. Step 0: complete proof of both halves; genbg finds exactly one β = 1 core for each n ≤ 7 |
| L9 | Bundles ≤ 2: EFX₀ ⟺ every good in another's 2-good bundle is worth ≤ own bundle | PROVED | `proofs/lemmas.md` | | basis of `src/verify_fail.py`. Step 0: complete proof; agrees with the raw definition on all 4,082,400 size-≤ 2 allocations of the X2 hypergraph |
| L10 | Insertion lemma | PROVED | `proofs/lemmas.md` | | Step 0: complete proof |
| L11 | Cores are subdivisions of finitely many shapes per β; β = 2: theta, dumbbell, figure-eight | PROVED | `proofs/lemmas.md` | | Step 0: complete proof; checked on 7,118 cores. β = 2 cores with n = 5, 6, 7 split into theta / dumbbell / figure-eight as 8/6/1, 11/12/2, 15/20/2 |
| S2.A | Collisions vs. P-capacity (plan Step 2): an EFX₀ allocation with all bundles ≤ 2 and e empty bundles leaves ≥ δ contested tops alone, so δ ≤ σ − 2e (δ = #contested tops − P-capacity, σ = 2n − m) | PROVED | `proofs/construction.md` | | §2. Necessary only, and never the binding reason for n ≤ 5: every C2-failing profile there has δ ≤ σ; what forces the large bundle is second-order (losers that need their b alone too) |
| S2.S | Construction LB is sound: any allocation it returns is EFX₀ with ≤ 1 bundle of > 2 goods; that bundle has #NA − σ + 2 goods (NA = the goods some agent needs alone) | PROVED | `proofs/construction.md`, `src/construct.py` | | §3, Theorems 1–2, directly from the EFX₀ definition (no L5). LB can fail only in its last step (no owner for the overflow bundle) |
| R1 | EFX₀ exists for every instance with ≤ 3 relevant goods per agent and n ≤ 6 | CERTIFIED | `results/certs_5_6.json.gz`, `results/frontier_results_5_6.json`, `results/enum_crosscheck.log`, `results/certs_small_m.json.gz`, `results/frontier_results_small_m.json`, `results/frontier_small_m.log`, `results/enum_crosscheck_small_m.log` | | no longer conditional on Mahara (Step 0): all 3,182 connected cores with m ≤ n + 3 certified directly; with L8 (m = 2n) every connected core with n ≤ 6 is covered (`proofs/lemmas.md`, last section). 251 + 3,182 connected cores, enumeration reproduced independently (nauty genbg and gen_cores, bijection up to isomorphism); CI re-checks without SAT |
| R2 | Conjecture D holds for every connected core with n = 7 and 11 ≤ m ≤ 13 | CERTIFIED | `results/certs_7_13.json.gz`, `results/certs_7_12.json.gz`, `results/certs_7_11.json.gz`, `results/frontier_results_7.json`, `results/frontier_results_7_12.json`, `results/frontier_results_7_11.json`, `results/check_certs_frontier.log`, `results/check_enum.log`, `results/enum_crosscheck.log` | | m = 13, 12, 11: 37, 541, 3,103 cores, each with an allocation in model C3 for every one of the 6^7 profiles (with L8 for m = 14: every connected core with n = 7 and m ≥ 11). Lists complete by orbit counting (`tools/check_enum.py`, independent of nauty; m = 13 also reproduced by gen_cores). Certificates re-checked without SAT, D shape included (`check_certs.py --require-d`): CI re-checks m = 13, 12 and all orbit counts; m = 11 (2.5 min on 4 CPUs) is re-checked locally, see the log |
| R3 | Conjecture D holds for every core with n ≤ 6, connected or not | CERTIFIED | `results/certs_5_6.json.gz`, `results/certs_small_m.json.gz`, `results/certs_c2_2_3_4.json.gz`, `results/certs_disconnected_6_10.json.gz`, `src/step0_checks.py`, `src/c2_small.py`, `results/step0_checks.log`, `results/c2_small.log`, `proofs/lemmas.md` | | Step 0. Connected cores: every stored allocation has ≤ 1 bundle of > 2 goods ("D shape" in `src/step0_checks.py`, and `tools/check_certs.py --require-d` in CI), plus L8 for m = 2n. Disconnected: at most two components with ≥ 3 agents; all 2-agent cores pass with bundles ≤ 2, and the one 3-agent core that does not (H3) is certified in H3 ⊔ H3 over all 6⁶ profiles (coverage re-checked with `tools/check_certs.py`'s raw masks) |
| R4 | Conjecture D holds for every connected core with n = 8 and m ∈ {14, 15} | CERTIFIED | `results/certs_8_15.json.gz`, `results/certs_8_14.json.gz`, `results/frontier_results_8_15.json`, `results/frontier_results_8_14.json`, `results/check_certs_frontier.log`, `results/check_enum.log` | | m = 15 (β = 2; also D2), 14 (β = 3): 52, 1,232 cores, each with an allocation in model C3 for every one of the 6^8 profiles (m = 16 by L8). Lists complete by orbit counting. Certificates re-checked without SAT, D shape included: CI re-checks m = 15 and the orbit counts; m = 14 (7.5 min on 4 CPUs) is re-checked locally, see the log. n = 8, m ≤ 13 not run |
| R5 | EFX₀ exists for every instance with ≤ 3 relevant goods per agent and n ≤ 7 | CERTIFIED | `results/certs_7_13.json.gz`, `results/certs_7_12.json.gz`, `results/certs_7_11.json.gz`, `results/check_enum.log`, `proofs/lemmas.md`, `proofs/citations.md` | | uses Mahara's theorem (m ≤ n + 3; read in full, EFX₀ form, `proofs/citations.md` item 1) for cores with 7 agents and at most 10 goods, the only ones not searched (37,488 connected cores). Argument of R1 (`proofs/lemmas.md`, last section): every connected component of a core with ≤ 7 agents has n′ ≥ 2 agents and m′ goods; n′ ≤ 6 is R1, m′ = 2n′ is L8, (7, 11), (7, 12), (7, 13) is R2, and n′ = 7, m′ ≤ 10 is Mahara (already in EFX₀ form, so L1 is not needed) |
| S2.N6 | Construction LB never fails on a core with n ≤ 6 (connected or not, every m); a second, constructive certification of R3 | CERTIFIED | `results/certs_lb_2_6.json.gz`, `results/certs_lb_disconnected_4_6.json.gz`, `results/check_certs_lb.log`, `results/construct_2_5.log`, `results/construct_6.log`, `results/construct_6_cert.log`, `results/construct_disconnected_4_6.log`, `results/enum_crosscheck_all_m.log` | | 3,436 connected and 131 disconnected cores, all 6^n profiles each. Every output checked against the raw definition in `src/construct.c`; C and Python implementations give identical outputs (all cores with n ≤ 5, every 10th with n = 6, all disconnected); certificates made of LB's own outputs re-checked by `tools/check_certs.py` and `src/construct_run.py --check-cert` (CI); enumeration cross-checked at every (n, m), n ≤ 6. `proofs/construction.md` §4 |
| X1 | Bundles ≤ 2 always suffice in a core | REFUTED | `results/exhaust5.log`, `proofs/counterexamples.md`, `src/c2_small.py`, `results/c2_small.log` | | n = 5: 1,226 of 93,312 profiles. Smallest (Step 0): n = 3, m = 5, 14 of 216 profiles (core H3, hand proof in `proofs/counterexamples.md`), found by SAT and by a SAT-free brute force (`src/c2_small.py`); no 2-agent core fails |
| X2 | Conjecture A: m ≤ 2n − 2 ⟹ bundles ≤ 2 suffice | REFUTED | `results/frontier_results_5_6.json`, `src/verify_fail.py`, `proofs/counterexamples.md`, `src/c2_small.py`, `results/c2_small.log` | | n = 6, m = 10: 57 of 211 cores; two independent encodings. Smallest (Step 0): n = 4, m = 6, 3 of 16 cores (hand proof for one), SAT and SAT-free brute force agree (`src/c2_small.py`) |
| X3 | Conjecture A for non-core instances | REFUTED | `proofs/counterexamples.md` | | four identical agents + three worthless goods |
| X4 | One bundle of 3 goods always suffices | REFUTED | `results/frontier_results_5_6.json`, `proofs/construction.md`, `results/large_bundle_structure.log` | | n = 6, m = 11: 3 of 25 cores need ≥ 4; confirmed by a second, independently written encoding that enumerates every allocation with one large bundle (`src/large_bundle.py structure`: 4 profiles, in exactly 3 cores, need ≥ 4; `results/large_bundle_structure.log`). Also at m ≤ 2n − 2, where the frontier never tested three: n = 6, m = 10, 78 profiles in 7 cores need exactly 4 (same log). Smaller and hand-checkable (Step 2): n = 4, m = 7, agents (0,2,3), (0,2,4), (1,2,5), (1,2,6); every EFX₀ allocation with ≤ 1 large bundle has sizes (4,1,1,1) (hand proof, `proofs/construction.md` §1; brute force over all 4⁷ allocations) |
| D2.C | Collector theorem: agents with a private good are edges of a multigraph on shared goods; if #E + #pins = #V + 1 and a cover state exists, then with one collector (the only bundle that may exceed two goods) every agent of E is safe | PROVED | `proofs/beta2.md`, `src/test_collector.py`, `results/test_collector.log` | | potential argument: each switch lowers the number of bad agents by one. Random test beyond β = 2: 164,861 multigraphs with pins, 0 failures |
| D2.O | Lemma O: a Q-agent z can hold only its top good while every other agent holds two own goods, whenever the component of a_z in G − z is a tree | PROVED | `proofs/beta2.md` | | Hall's theorem; all bundles ≤ 2 |
| D2 | Conjecture D holds for every connected core with β = 2 (m = 2n − 1), for all n ≥ 2 | PROVED | `proofs/beta2.md`, `src/beta2.py`, `src/verify_beta2.py`, `results/verify_beta2.log` | | constructive, self-contained (re-derives the parts of L4, L5, L8 it uses). Cross-check: the proof run as an algorithm on every β = 2 core and profile, n = 2–8 (98,991,756 core–profile pairs), every output EFX₀ by the raw definition, 0 failures; certificates `results/certs_beta2_{2..8}.json.gz` accepted by `tools/check_certs.py` (`results/check_certs_beta2.log`). Includes n = 2, 3, 4, not covered by R1 |
| D | Every core has an EFX₀ allocation with at most one bundle of more than two goods | CONJECTURE | | | certified for every core with n ≤ 6, connected or not (R3; disconnected cores need a separate argument, note after L6 in `proofs/lemmas.md`), and for every connected core with n = 7, m ≥ 11 (R2) or n = 8, m ≥ 14 (R4; m = 2n by L8). For TARGET, D for connected cores suffices. Proved for connected cores with β = 2 (m = 2n − 1), all n (D2). Not checked: connected cores with n = 7, m ≤ 10 or n = 8, m ≤ 13; disconnected cores with n ≥ 7. Construction LB gives it for n ≤ 6 (S2.N6), finds it for every connected core with n = 7, m = 10 (exhaustive, raw-checked, no stored certificate: S2.LB) and would give it in general (S2.LB) |
| S2.LB | Construction LB (`src/construct.py`) never fails, on every core; with S2.S this implies D | CONJECTURE | | | certified for n ≤ 6 (S2.N6). Exhaustive with 0 failures, every output checked against the raw definition in `src/construct.c` but no stored certificate: n = 7 at m = 10–14 (3,429,495,936 core–profile pairs, `results/construct_7.log`), n = 8 at m = 15, 16 (89,019,648 pairs, `results/construct_8.log`). The open step is the last one: an owner for the overflow bundle that holds no b_k, c_k pair of an agent holding only its top (`proofs/construction.md` §3) |
| S2.K | Canonical large bundle: if a core has no EFX₀ allocation with all bundles ≤ 2, it has one whose only large bundle belongs to an agent o in case C and consists of c_o and goods private to agents in case T or B | CONJECTURE | | | true in every C2-failing (core, profile) with n ≤ 6 (full enumeration, `results/large_bundle_structure.log`) and n = 7, m = 13 (SAT with the shape imposed, `results/large_bundle_canon.log`); every witness re-checked against the raw definition. The shape of D2.C's collector, beyond β = 2; `proofs/construction.md` §5.2 |
| T | TARGET: EFX₀ exists whenever every agent has ≤ 3 relevant goods | OPEN | | | implied by D; holds for n ≤ 7 (R1, R5) and for the core classes of T2, T3, T4 (every core component with β ≤ 3). A minimal counterexample has β ≥ 4 and n ≤ 5(β − 1) − t (MC4) |
| T2 | TARGET holds for every instance whose core has only connected components with β ≤ 2 | PROVED | `proofs/beta2.md` | | D2 with L2, L3, L6, L8 (complete proofs in `proofs/lemmas.md`, Step 0) |
| T3 | TARGET holds for every instance each of whose core components has β ≤ 2, or m ≤ n + 3, or no good relevant to three or more of its agents | PROVED | `proofs/citations.md`, `proofs/beta2.md` | | uses two published theorems, read in full (`proofs/citations.md`): Mahara 2107.09901, Theorem 3 (m ≤ n + 3, monotone valuations, EFX₀ form) and Afshinmehr et al. 2606.18665 (multigraphs, cancelable valuations, EFX₀ form; embedding of private goods in `proofs/citations.md` item 4); with T2 and L6. Existence only: says nothing about D. Left open for TARGET: connected cores with β ≥ 3, m ≥ n + 4 and a good relevant to ≥ 3 agents |
| T4 | TARGET holds for every instance each of whose core components has β ≤ 3 (m_C ≥ 2n_C − 2) | CERTIFIED | `proofs/min_counterexample.md`, `results/certs_min_cex_9_16.json.gz`, `results/certs_min_cex_10_18.json.gz`, `results/cores_9_16_10_18.json.gz`, `results/min_cex_cores_9_16.log`, `results/min_cex_cores_10_18.log`, `results/check_enum_min_cex.log`, `results/check_certs_min_cex.log`, `src/min_cex_cores.py`, `tools/check_min_cex_cores.py` | | Theorem M5: a minimal counterexample within 𝒞_3 is a connected core with β = 3; n ≤ 8 is covered by R1, R2, R4, and MC3 + MC4 give n ≤ 10. The 2,477 + 4,619 connected cores with (n, m) = (9, 16), (10, 18) (complete by orbit counting) include exactly 15 + 5 allowed by MC3; each is certified under all 6^n profiles in model C3, so D holds for these 20 too (re-checked without SAT, D shape included). Uses no published theorem (not T3). Extends T2 from β ≤ 2 |
| MC1 | Local reductions are sound: if every admissible local state of the smaller instance has an extension (Lemma M1, M1(b) with an unenvied bundle), the configuration does not occur in a minimal counterexample, nor in one within 𝒞_k | PROVED | `proofs/min_counterexample.md` | | §1–§2: plain EFX₀ is the inductive statement (closed under sub-instances and gadgets); 𝒞_k = instances whose incidence-graph components have cyclomatic number ≤ k, closed under the reductions used. Checker: `src/reduce.py` |
| MC2 | A minimal counterexample has no agent whose top good is its private good and that shares a good of degree 2 with a P-agent | PROVED | `proofs/min_counterexample.md` | | Lemma M2, hand proof (peel the pair); also certified as families RPT-e/RPT-f of MC3 |
| MC3 | A minimal counterexample (or one within 𝒞_k) has no good of degree 2 valued by two P-agents; every thread carries at most one P-agent | CERTIFIED | `proofs/min_counterexample.md`, `src/reduce.py`, `src/min_cex.py`, `results/min_cex_reductions.json.gz`, `results/min_cex_reductions.log`, `tools/check_reductions.py`, `results/check_reductions.log` | | Theorem M3: configurations pair (36 profiles) and loop (36), each profile reduced by peeling (RPT), deletion (DEL), contraction (CON) or a one-agent gadget (GAD). 183 reductions, 10,066 local states with extensions; re-checked by a second implementation (different state enumeration, L5 case table instead of the raw definition), which also re-derives the coverage; tamper self-test in the log |
| MC4 | A minimal counterexample has n ≤ 5(β − 1) − t, i.e. 5m ≤ 9n − t (t = Σ over goods of degree ≥ 3 of deg − 2) | CERTIFIED | `proofs/min_counterexample.md` | | Corollary M4: counting (proved) on top of MC3. With T3: β + 3 ≤ n ≤ 5β − 6 |
| MC6 | In a minimal counterexample, a P-agent sharing a good of degree 2 with a Q-agent does not rank its private good last | CERTIFIED | `proofs/min_counterexample.md`, `results/min_cex_reductions.json.gz`, `results/check_reductions.log` | | Lemma M6 (configuration pq, contraction with an unenvied bundle; 14 of 36 profiles reduce, the 12 claimed re-checked). Not used by T4. Failed reductions for the other 22: `attempts/min-cex-pq-reductions.md` |

## Lessons
- About 3,000 random cores "supported" conjecture A; exhaustive search refuted it in minutes (failures: 1 to 747 of 46,656 profiles per hypergraph, median 22; `results/fail_density_6_10.log`. An earlier sample of three hypergraphs showed 2 to 36).
- Every UNSAT claim that refutes a conjecture gets a second, independently written encoding before it enters this ledger.
- Delegating a range to an external theorem hid the smallest counterexamples. Bundles ≤ 2 already fail at n = 3, and conjecture A at n = 4, in the range m ≤ n + 3 that had been left to Mahara and never searched. Searching that range took 70 s.

## Open items
1. ~~Independent re-implementation of the core enumeration (the trust point of R1): reproduce 15 / 211 / 25 / 37.~~ Done: `src/cores_nauty.py` (nauty genbg) matches `gen_cores` up to isomorphism, `results/enum_crosscheck.log`.
2. ~~Verify the Mahara citation (m ≤ n + 3).~~ Done: read in full (arXiv 2107.09901v2, Theorem 3; general monotone valuations, EFX₀ form), `proofs/citations.md`. R1 does not depend on it. R5 uses it for 7-agent cores with ≤ 10 goods, and T3 for every n.
3. ~~Regenerate the n = 7, m = 13 certificates; run m = 12 and 11; then n = 8 at m = 15 and 14~~ Done (R2, R4; logs `results/frontier7_12.log`, `results/frontier7_11.log`, `results/frontier8_15.log`, `results/frontier8_14.log`): measured on 4 CPUs, n = 7: m = 12 in 46 s, m = 11 in 11 min; n = 8: m = 15 in 20 s, m = 14 in 33 min (roughly the first half on shared CPUs; the estimate was ~20 min). Not run, estimated from samples on 4 CPUs and probably low given m = 14: n = 8, m = 13: 11,478 cores, ~13 h; m = 12: 52,889 cores, ~4 days. Also not run: n = 7, m ≤ 10 (37,488 connected cores); certifying them would make R5 independent of the literature, as Step 0 did for n ≤ 6.
4. ~~Characterize the large bundle (Step 2).~~ Done for n ≤ 6: `proofs/construction.md` (structure, S2.A, construction LB). ~~Attempt D for β = 2 via L11~~ done: D2, `proofs/beta2.md` (collector theorem and Lemma O; no path shortening was needed).
5. Conjecture D for disconnected cores in general: L6 does not carry D's shape across components (`proofs/lemmas.md`, note after L6). Certified for n ≤ 6 (R3), open beyond.
6. ~~Read the sources in `proofs/citations.md`, including arXiv 2608.03171 and 2608.30203.~~ Done for seven papers (texts supplied by the repository owner): none implies TARGET or D; two give T3. Viswanathan–Mehta read too: ordinary EFX only (removed good must be worth > 0), so it does not imply TARGET. Still [unverified]: Christodoulou et al. (EC 2023), Chaudhury–Garg–Mehlhorn (JACM 2024), and the leads listed as not read.
7. Conjecture D for β = 3 (m = 2n − 2, plan Step 3.2): the collector theorem (D2.C) absorbs exactly one deficit unit, and a β = 3 core without Q-agents has two; see `proofs/beta2.md` §6.
8. Prove S2.LB (construction LB never fails; its last step is the gap), or find the smallest core where it fails.
9. Minimal counterexample (plan Step 3.3, `proofs/min_counterexample.md`): TARGET is now open only for β ≥ 4 (T4), and a minimal counterexample satisfies β + 3 ≤ n ≤ 5β − 6 (MC4 with T3). Next: β = 4 leaves cores with 8 ≤ n ≤ 14 satisfying MC3 and MC6; listing them needs a kernel-based generator (genbg lists all cores), and n = 8, m = 13 is not certified. Reductions at Q-agents and at goods of degree ≥ 3 would be needed for unavoidability; the ones tried fail (`attempts/min-cex-pq-reductions.md`).
