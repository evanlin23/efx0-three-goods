# Ledger: EFX₀ with at most three relevant goods per agent

The single source of truth. A status changes only in a pull request that adds the artifact in the Artifact column; CI checks that every PROVED, CERTIFIED or REFUTED row points to files that exist. The Lean column names machine-checked statements in `lean/` (core Lean, no `sorry`, standard axioms only; see `lean/README.md`); CI checks that each has a `#print axioms` certificate and that `lean/check.sh` passes.

| ID | Claim | Status | Artifact | Lean | Notes |
|---|---|---|---|---|---|
| L1 | For fixed (n, m): EFX₀ for all additive instances ⟺ EFX for all positive additive instances | PROVED | `proofs/lemmas.md` | | Step 0: complete proof. Transfers only results stated for classes of (n, m), not results about few relevant goods or (multi)graphs. No longer used by R1 |
| L2 | Peeling rules R1 and R2 | PROVED | `proofs/lemmas.md` | `EFX.peel`, `EFX.peelEmpty`, `EFX.peelR2`, `EFX.peelBundle` | Lean (over lists): R1, R1 with P = ∅, R2 (without its unused hypothesis that P has ≥ 2 goods), and the general peeling step that R2 and R1 with P = ∅ instantiate. `src/lemmas.py`: 1,500 random instances through the pipeline, 0 failures. Step 0: complete proof of the general peeling step (P worth ≥ i's remaining goods; |P| ≤ 1 or P worthless to the others); `src/step0_checks.py`: exhaustive on two small grids, 0 failures |
| L2c | ≤ 2 relevant goods per agent ⟹ EFX₀ (serial dictatorship; the HW1 theorem) | PROVED | `proofs/lemmas.md` | `EFX.exists_efx0_of_count` | stronger form (all bundles but one have ≤ 1 good) machine-checked in evanlin23/mrd-efx: `MRD.main_theorem_L`. Step 0: complete proof, and of the brief's remark on when serial dictatorship breaks at three goods |
| L3 | Worthless goods go to an envy-graph source | PROVED | `proofs/lemmas.md` | `EFX.junk`, `EFX.Inst.exists_efx0_of_junk`, `EFX.rotate_efx0`, `EFX.exists_unenvied` | same pipeline test. Lean: over lists and in the model; rotation preserves EFX₀, and rotating envy cycles reaches an allocation with an unenvied agent. Step 0: complete proof; `src/step0_checks.py`: exhaustive on two small grids, 0 failures |
| L4 | Core counting: 3n = 2m − π + Σ(deg − 2), so m ≤ 2n | PROVED | `proofs/lemmas.md` | | Step 0: complete proof; identity checked on 7,118 enumerated cores (`src/step0_checks.py`) |
| L5 | In a core, EFX₀ is ordinal and equals cases T/P/B/C/E | PROVED | `proofs/lemmas.md` | | `src/coreG.py`: 896,400 allocations vs. the definition, 0 mismatches. Step 0: complete proof; with ties the cases stay sufficient, so tied cores reduce to strict profiles. `src/step0_checks.py`: all 47 local configurations × 6 rankings × 95 realizations vs. the raw definition, 0 mismatches |
| L6 | Disconnected cores are solved component by component | PROVED | `proofs/lemmas.md` | | Step 0: complete proof. It composes EFX₀ allocations, but not conjecture D's shape (see D) |
| L7 | β = 2n − m + 1; alone-goods identity | PROVED | `proofs/lemmas.md` | | Step 0: complete proof; β checked on 7,118 cores, the alone-goods identity on all 5,670 size-≤ 2 partitions of the X2 goods |
| L8 | Two own goods ⟹ safe; β = 1 cores solved by orientation | PROVED | `proofs/lemmas.md` | `EFX.Inst.safe_of_two_own`, `EFX.envyFree_of_two_own`, `EFX.Inst.efx0_of_two_own`, `EFX.balance_needed` | Lean: the first half, for agents with a ≤ b + c (all core agents), and "every agent holds two own goods ⟹ EFX₀". Balance is necessary (`EFX.balance_needed`: a top-heavy counterexample). Not formalized: that a β = 1 core admits such an allocation. Step 0: complete proof of both halves; genbg finds exactly one β = 1 core for each n ≤ 7 |
| L9 | Bundles ≤ 2: EFX₀ ⟺ every good in another's 2-good bundle is worth ≤ own bundle | PROVED | `proofs/lemmas.md` | | basis of `src/verify_fail.py`. Step 0: complete proof; agrees with the raw definition on all 4,082,400 size-≤ 2 allocations of the X2 hypergraph |
| L10 | Insertion lemma | PROVED | `proofs/lemmas.md` | | Step 0: complete proof |
| L11 | Cores are subdivisions of finitely many shapes per β; β = 2: theta, dumbbell, figure-eight | PROVED | `proofs/lemmas.md` | | Step 0: complete proof; checked on 7,118 cores. β = 2 cores with n = 5, 6, 7 split into theta / dumbbell / figure-eight as 8/6/1, 11/12/2, 15/20/2 |
| L12 | Real values reduce to natural numbers: for nonnegative real additive valuations v in which every agent has ≤ 3 relevant goods, there are natural-number additive valuations w with the same relevant goods such that v_i(S) ≤ v_i(T) ⟺ w_i(S) ≤ w_i(T) for every agent i and all sets S, T; so an allocation is EFX₀ for w iff it is for v (same bundles, so the D shape carries over), and balance and cores are preserved | PROVED | `proofs/real_values.md`, `lean/EFX/RealValues.lean` | `EFX.l12`, `EFX.OrderedValue.exists_agree`, `EFX.OrderedValue.tri_rep`, `EFX.numRelevant_eq_of_agree`, `EFX.OrderedValue.balanced_iff_of_agree`, `EFX.efx0_iff_of_agree` | per agent: after cancelling the common goods, every comparison is between disjoint subsets of the ≤ 3 relevant goods a ≥ b ≥ c > 0; since b < a + c and c < a + b always, the order is fixed by the ties among a, b, c and the sign of a − (b + c); one natural-number representative per pattern (e.g. (4, 3, 2), (3, 2, 1), (5, 2, 1), (2, 2, 1), (3, 2, 2), (2, 1, 1), (3, 1, 1), (1, 1, 1), (2, 1), (1, 1), (1)). Extends `EFX.target` and `EFX.LB.corollaryD` (natural-number values) to real values. Machine-checked in Lean (`formal/real-values`, `lean/EFX/RealValues.lean`) for values in any type satisfying `EFX.OrderedValue` (0, +, ≤: + associative and commutative with identity 0, ≤ a total order, a ≤ b ⟺ a + c ≤ b + c; ℝ≥0, ℚ≥0, ℕ satisfy these), nonnegative values as a hypothesis: `EFX.l12` gives w with the same relevant goods and the same answer to every comparison between two subset sums (`EFX.Agree`); the per-agent pattern argument is `EFX.OrderedValue.exists_agree`, with the eight representatives (permuted, 31 triples) checked by `decide +kernel` (`EFX.Pat.table`) and the phantom-slot trick for agents with one or two relevant goods; EFX₀, the relevant-goods count and balance transfer (`EFX.efx0_iff_of_agree`, `EFX.numRelevant_eq_of_agree`, `EFX.OrderedValue.balanced_iff_of_agree`). The remark that cores are preserved (K1–K4) is written only (`proofs/real_values.md`), not formalized |
| S2.A | Collisions vs. P-capacity (plan Step 2): an EFX₀ allocation with all bundles ≤ 2 and e empty bundles leaves ≥ δ contested tops alone, so δ ≤ σ − 2e (δ = #contested tops − P-capacity, σ = 2n − m) | PROVED | `proofs/construction.md` | | §2. Necessary only, and never the binding reason for n ≤ 5: every C2-failing profile there has δ ≤ σ; what forces the large bundle is second-order (losers that need their b alone too) |
| S2.S | Construction LB is sound: any allocation it returns is EFX₀ with ≤ 1 bundle of > 2 goods; that bundle has #NA − σ + 2 goods (NA = the goods some agent needs alone) | PROVED | `proofs/construction.md`, `src/construct.py` | `EFX.LB.lb_sound_model`, `EFX.LB.lb_sound`, `EFX.LB.sound` | §3, Theorem 1 (soundness) and Lemma 2 (size of the large bundle), directly from the EFX₀ definition (no L5). LB can fail only in its last step (no owner for the overflow bundle). Lean: Theorem 1 (not Lemma 2), both in the abstract form (`EFX.LB.sound`: picks with (I1), upgraded/frozen/slot-filled bundles and the owner constraint ⟹ EFX₀ for every consistent valuation, ties allowed, ≤ 1 large bundle) and for LB itself (`EFX.LB.lb_sound_model`), which is defined in Lean as in `construct.py` except that Phase 1's processing order is an argument: the theorem holds for every order, so LB's order heuristic is not formalized. That the Lean `lb` computes what `construct.py` computes is evidence: same allocation on 1,809,209 core–profile pairs (all with n ≤ 4, samples with n = 5, 6; `lean/scripts/lb_crosscheck.py`, `lean/scripts/lb_crosscheck.log`) |
| S2.R | Owner r (Theorem A): after any run of LB's Phase 1 (any choices at R1 and insertion steps) and LB's upgrades (in any order), if the last-processed agent r that is not upgraded is not a valid owner of the overflow bundle (no set H satisfies Lemma 1's condition), then the "bad case" holds: an agent k of r's block (a block leader) picked its top, {b_k, c_k} ⊆ J ∪ {Y_r}, k is frozen, the junk parts of the pairs exposed for r are pairwise disjoint, and every need chain from k ends at r | PROVED | `proofs/lb_last_step.md`, `lean/EFX/OwnerR.lean`, `lean/EFX/Blocks.lean`, `lean/EFX/LBPlus.lean`, `src/lbplus.c`, `src/lbplus.py`, `results/lbplus.log` | `EFX.LB.theoremA_invalid`, `EFX.LB.validOwner_iff`, `EFX.LB.theoremA`, `EFX.LB.kstar_spec`, `EFX.LB.exposed_lead`, `EFX.LB.phase1_run`, `EFX.LB.upFinal_valid`, `EFX.LB.state_of_final` | written proof: `proofs/lb_last_step.md` (Theorem A, §4); two independent reviews found no error. Machine-checked in Lean (`formal/lbplus`, PR #18), stated over the Lean definitions of LB's state (Blocks/OwnerR/PreAlloc/LBPlus), which a reader must check against the text; any run of Phase 1 and the upgrades gives the state Theorem A assumes (`EFX.LB.phase1_run`, `EFX.LB.upFinal_valid`, `EFX.LB.state_of_final`). "Valid owner" is Lemma 1's condition for some H (`EFX.LB.ValidOwner`); it holds exactly when an explicit hitting set (one good per exposed pair, a shared good for one meeting pair) fits (`EFX.LB.validOwner_iff`), so the test is computable. k is the agent exposed for r in r's block; that this is the last block ((A2)) is not needed and not formalized. Exposed agents are block leaders, one per block, and need chains inject them into slot-holding agents. Cross-check (evidence): `src/lbplus.c` asserts every step on all runs of S2.LB+, 0 failures |
| S2.LB+ | Construction LB⁺ (LB's Phase 1 with any choices at R1 and insertion steps, LB's upgrades in any order, owner r if r is a valid owner, or else one rotation along any need chain from k to r and owner k; any completions satisfying (OC)) never meets an undefined case (an output always exists, and in the rotation branch every need chain from k to r gives one), and every allocation it returns is EFX₀ with at most one bundle of > 2 goods, for every instance in which every agent values exactly three goods and is balanced | PROVED | `proofs/lb_last_step.md`, `lean/EFX/LBPlus.lean`, `lean/EFX/Rotation.lean`, `lean/EFX/PreAlloc.lean`, `lean/EFX/Blocks.lean`, `src/lbplus.c`, `src/lbplus.py`, `results/lbplus.log` | `EFX.LB.lbPlusRun_sound`, `EFX.LB.lbPlusOut_exists`, `EFX.LB.lbPlusOut_exists_chain`, `EFX.LB.lbPlus_sound_model`, `EFX.LB.lbPlus_sound`, `EFX.LB.theoremB`, `EFX.LB.Valid.sound` | written proof: `proofs/lb_last_step.md` (Theorems 1′, B, C); two independent reviews found no error. Machine-checked in Lean (`formal/lbplus`, PR #18), stated over the Lean definitions of LB's state (Blocks/OwnerR/PreAlloc/LBPlus), which a reader must check against the text: Theorem 1′ for every completion of a valid pre-allocation that satisfies (OC) (`EFX.LB.Valid.sound`), Theorem B for any need chain (`EFX.LB.theoremB`), and Theorem C for LB⁺ over all its choices (`EFX.LB.LBPlusRun`: every output is EFX₀ for every valuation consistent with the rankings, a ≥ b ≥ c > 0 and a ≤ b + c, ties allowed, with ≤ 1 large bundle, `EFX.LB.lbPlusRun_sound`; an output always exists, `EFX.LB.lbPlusOut_exists`; in the rotation branch every need chain from k* to r gives an output, `EFX.LB.lbPlusOut_exists_chain`) and for the computable `EFX.LB.lbPlus` (one of the outputs; `decide` examples in `lean/EFX/LBPlus.lean`). Does not use connectivity, the private-good condition, L5 or any computation. Cross-check (evidence, `results/lbplus.log`): LB⁺ run literally by `src/lbplus.c`, every lemma asserted, every output checked against the raw EFX₀ definition. Covers every core with n ≤ 6 under every ranking profile and every sequence of insertion choices (2.47 × 10⁹ runs, 370,342 rotations). It also covers LB's own runs and random insertion/R1 choices on every connected core with n = 7, 11 ≤ m ≤ 14 and n = 8, m ∈ {15, 16}, plus random non-core instances with n = 5–7: 5.84 × 10⁹ runs in all, 1,327,101 rotations, 0 failures. The independent Python implementation `src/lbplus.py` gives identical tallies on all 40 levels with n ≤ 5 |
| R1 | EFX₀ exists for every instance with ≤ 3 relevant goods per agent and n ≤ 6 | CERTIFIED | `results/certs_5_6.json.gz`, `results/frontier_results_5_6.json`, `results/enum_crosscheck.log`, `results/certs_small_m.json.gz`, `results/frontier_results_small_m.json`, `results/frontier_small_m.log`, `results/enum_crosscheck_small_m.log` | | no longer conditional on Mahara (Step 0): all 3,182 connected cores with m ≤ n + 3 certified directly; with L8 (m = 2n) every connected core with n ≤ 6 is covered (`proofs/lemmas.md`, last section). 251 + 3,182 connected cores, enumeration reproduced independently (nauty genbg and gen_cores, bijection up to isomorphism); CI re-checks without SAT |
| R2 | Conjecture D holds for every connected core with n = 7 and 11 ≤ m ≤ 13 | CERTIFIED | `results/certs_7_13.json.gz`, `results/certs_7_12.json.gz`, `results/certs_7_11.json.gz`, `results/frontier_results_7.json`, `results/frontier_results_7_12.json`, `results/frontier_results_7_11.json`, `results/check_certs_frontier.log`, `results/check_enum.log`, `results/enum_crosscheck.log` | | m = 13, 12, 11: 37, 541, 3,103 cores, each with an allocation in model C3 for every one of the 6^7 profiles (with L8 for m = 14: every connected core with n = 7 and m ≥ 11). Lists complete by orbit counting (`tools/check_enum.py`, independent of nauty; m = 13 also reproduced by gen_cores). Certificates re-checked without SAT, D shape included (`check_certs.py --require-d`): CI re-checks m = 13, 12 and all orbit counts; m = 11 (2.5 min on 4 CPUs) is re-checked locally, see the log |
| R3 | Conjecture D holds for every core with n ≤ 6, connected or not | CERTIFIED | `results/certs_5_6.json.gz`, `results/certs_small_m.json.gz`, `results/certs_c2_2_3_4.json.gz`, `results/certs_disconnected_6_10.json.gz`, `src/step0_checks.py`, `src/c2_small.py`, `results/step0_checks.log`, `results/c2_small.log`, `proofs/lemmas.md` | | Step 0. Connected cores: every stored allocation has ≤ 1 bundle of > 2 goods ("D shape" in `src/step0_checks.py`, and `tools/check_certs.py --require-d` in CI), plus L8 for m = 2n. Disconnected: at most two components with ≥ 3 agents; all 2-agent cores pass with bundles ≤ 2, and the one 3-agent core that does not (H3) is certified in H3 ⊔ H3 over all 6⁶ profiles (coverage re-checked with `tools/check_certs.py`'s raw masks) |
| R4 | Conjecture D holds for every connected core with n = 8 and m ∈ {14, 15} | CERTIFIED | `results/certs_8_15.json.gz`, `results/certs_8_14.json.gz`, `results/frontier_results_8_15.json`, `results/frontier_results_8_14.json`, `results/check_certs_frontier.log`, `results/check_enum.log` | | m = 15 (β = 2; also D2), 14 (β = 3): 52, 1,232 cores, each with an allocation in model C3 for every one of the 6^8 profiles (m = 16 by L8). Lists complete by orbit counting. Certificates re-checked without SAT, D shape included: CI re-checks m = 15 and the orbit counts; m = 14 (7.5 min on 4 CPUs) is re-checked locally, see the log. n = 8, m = 13: construction LB (S2.N8); m ≤ 12 not run |
| R5 | EFX₀ exists for every instance with ≤ 3 relevant goods per agent and n ≤ 7 | CERTIFIED | `results/certs_7_13.json.gz`, `results/certs_7_12.json.gz`, `results/certs_7_11.json.gz`, `results/check_enum.log`, `results/certs_lb_7_3_7.json.gz`, `results/certs_lb_7_8.json.gz`, `results/certs_lb_7_9.json.gz`, `results/certs_lb_7_10.json.gz`, `results/check_certs_lb_7.log`, `proofs/lemmas.md` | | no longer conditional on Mahara: the 37,488 connected cores with 7 agents and at most 10 goods, earlier left to Mahara's theorem (m ≤ n + 3, `proofs/citations.md` item 1), are certified by the S2.N7 certificates (allocations checked from the raw definition, so valid in every labelling), as Step 0 did for n ≤ 6. Argument of R1 (`proofs/lemmas.md`, last section): every connected component of a core with ≤ 7 agents has n′ ≥ 2 agents and m′ goods; n′ ≤ 6 is R1, m′ = 2n′ is L8, (7, 11), (7, 12), (7, 13) is R2, and n′ = 7, m′ ≤ 10 is S2.N7 |
| S2.N6 | Construction LB never fails on a core with n ≤ 6 (connected or not, every m), in the labelling stored in the certificates (produced by `cores_nauty.py`; for disconnected cores by `construct_run.disconnected_cores`); a second, constructive certification of R3 | CERTIFIED | `results/certs_lb_2_6.json.gz`, `results/certs_lb_disconnected_4_6.json.gz`, `results/check_certs_lb.log`, `results/construct_2_5.log`, `results/construct_6.log`, `results/construct_6_cert.log`, `results/construct_disconnected_4_6.log`, `results/enum_crosscheck_all_m.log` | | 3,436 connected and 131 disconnected cores, all 6^n profiles each. Every output checked against the raw definition in `src/construct.c`; C and Python implementations give identical outputs (all cores with n ≤ 5, every 10th with n = 6, all disconnected); certificates made of LB's own outputs re-checked by `tools/check_certs.py` and `src/construct_run.py --check-cert` (CI); enumeration cross-checked at every (n, m), n ≤ 6. `proofs/construction.md` §4. LB breaks ties by index, so it is not invariant under relabelling: certified for one labelling per isomorphism class (R3 and D are invariant, so they do not depend on this). Other labellings are evidence only: five random relabellings of every connected core with n ≤ 6 (and one of every connected core with n = 7, m ≥ 11), 0 failures (`results/construct_relabel.log`) |
| S2.N7 | Construction LB never fails on a connected core with n = 7 (every m), in the labelling stored in the certificates (produced by `cores_nauty.py`); the certificates (LB's own outputs) give D for every connected core with n = 7, in every labelling, checked from the raw EFX₀ definition by `tools/check_certs.py --require-d` (a computational confirmation of D, which is proved; it uses neither S2.S nor the Lean proof) | CERTIFIED | `results/certs_lb_7_3_7.json.gz`, `results/certs_lb_7_8.json.gz`, `results/certs_lb_7_9.json.gz`, `results/certs_lb_7_10.json.gz`, `results/certs_lb_7_11_14.json.gz`, `results/construct_7_cert.log`, `results/check_certs_lb_7.log`, `results/construct_7_python_sample.log` | | 41,170 connected cores (m = 3–14; 37,488 of them with m ≤ 10), all 6^7 profiles each (1.15 × 10¹⁰ core–profile pairs), 0 failures. LB's non-failure is construct.c's exhaustive count (raw EFX₀ check of every output in the same program); C and Python agree on the sampled cores in `results/construct_7_python_sample.log` (every 200th core with m = 10–14). The certificates independently confirm D, not LB's non-failure (construct.c stores a first-fit cover and would skip a failing profile, which another stored output could still cover). The C code is S2.N6's, now built with -O3 -march=native (the certificate checks do not depend on the build); C and Python outputs are still identical on every core with n = 5 (CI step "Construction LB from scratch for n <= 5"). Certificates made of LB's own outputs, split by m (9.0 MB in all): `tools/check_certs.py --require-d` accepts all five (every profile covered, from the raw definition; D shape), and `tools/check_enum.py` finds every list complete by orbit counting; `construct_run.py --check-cert` also accepts m = 10. CI re-checks the orbit counts and the m ≤ 7 certificate; the others take 5–17 min each on 2 CPUs and are re-checked locally (see the log). LB needs a large bundle only at m ≥ 9. Same labelling caveat as S2.N6. Replaces Mahara's theorem in R5 |
| S2.N8 | Construction LB never fails on a connected core with n = 8 and m ≥ 13, in the labelling stored in the certificates (produced by `cores_nauty.py`); the certificates (LB's own outputs) give D for every connected core with n = 8 and m ≥ 13, in every labelling, checked from the raw EFX₀ definition by `tools/check_certs.py --require-d`, the first certification of D at n = 8, m = 13 (a computational confirmation of D, which is proved; it uses neither S2.S nor the Lean proof) | CERTIFIED | `results/certs_lb_8_13_part1.json.gz`, `results/certs_lb_8_13_part2.json.gz`, `results/certs_lb_8_13_part3.json.gz`, `results/certs_lb_8_13_part4.json.gz`, `results/certs_lb_8_14.json.gz`, `results/certs_lb_8_15_16.json.gz`, `results/construct_8_13.log`, `results/construct_8_14.log`, `results/construct_8_15_16.log`, `results/check_certs_lb_8.log`, `results/construct_8_python_sample.log` | | m = 13: 11,478 cores, run in four slices of the core list (`construct_run.py --part`); m = 14: 1,232; m = 15, 16: 53. All 6^8 profiles each (2.14 × 10¹⁰ core–profile pairs, about 7 h on 4 CPUs), 0 failures. LB's non-failure is construct.c's exhaustive count (raw EFX₀ check of every output in the same program); C and Python agree on the sampled cores in `results/construct_8_python_sample.log` (7 cores with m = 13, the first 3 and last 4 of the list, 1.2 × 10⁷ profiles). The certificates independently confirm D, not LB's non-failure. Certificates made of LB's own outputs (m = 13 in four parts of 3.4–4.4 MB; 17 MB in all): `tools/check_certs.py --require-d` accepts each, and `tools/check_enum.py` finds every list complete by orbit counting (m = 13 with `--union` over the four parts, which together list each core exactly once). CI re-checks the orbit counts and m = 15, 16; the m = 13 parts (about 70 min each on 2 CPUs) and m = 14 are re-checked locally (see the log). Same labelling caveat as S2.N6. Not run: n = 8, m ≤ 12 (52,889 cores at m = 12; about a day: 28 h at the logged 1.9 s per core on shared CPUs, about 20 h uncontended) |
| X1 | Bundles ≤ 2 always suffice in a core | REFUTED | `results/exhaust5.log`, `proofs/counterexamples.md`, `src/c2_small.py`, `results/c2_small.log` | | n = 5: 1,226 of 93,312 profiles. Smallest (Step 0): n = 3, m = 5, 14 of 216 profiles (core H3, hand proof in `proofs/counterexamples.md`), found by SAT and by a SAT-free brute force (`src/c2_small.py`); no 2-agent core fails |
| X2 | Conjecture A: m ≤ 2n − 2 ⟹ bundles ≤ 2 suffice | REFUTED | `results/frontier_results_5_6.json`, `src/verify_fail.py`, `proofs/counterexamples.md`, `src/c2_small.py`, `results/c2_small.log` | | n = 6, m = 10: 57 of 211 cores; two independent encodings. Smallest (Step 0): n = 4, m = 6, 3 of 16 cores (hand proof for one), SAT and SAT-free brute force agree (`src/c2_small.py`) |
| X3 | Conjecture A for non-core instances | REFUTED | `proofs/counterexamples.md` | | four identical agents + three worthless goods |
| X4 | One bundle of 3 goods always suffices | REFUTED | `results/frontier_results_5_6.json`, `proofs/construction.md`, `results/large_bundle_structure.log` | | n = 6, m = 11: 3 of 25 cores need ≥ 4; confirmed by a second, independently written encoding that enumerates every allocation with one large bundle (`src/large_bundle.py structure`: 4 profiles, in exactly 3 cores, need ≥ 4; `results/large_bundle_structure.log`). Also at m ≤ 2n − 2, where the frontier never tested three: n = 6, m = 10, 78 profiles in 7 cores need exactly 4 (same log). Smaller and hand-checkable (Step 2): n = 4, m = 7, agents (0,2,3), (0,2,4), (1,2,5), (1,2,6); every EFX₀ allocation with ≤ 1 large bundle has sizes (4,1,1,1) (hand proof, `proofs/construction.md` §1; brute force over all 4⁷ allocations) |
| X5 | Minimality of the last insertion suffices: in any run of LB's Phase 1 whose last insertion step inserts an agent of minimal lookahead count, r (S2.R) is a valid owner | REFUTED | `attempts/lb_last_insertion.py`, `attempts/lb-last-insertion-lookahead.md`, `results/lb_tree.log` | | true for n ≤ 5 (all insertion choices); fails at n = 6, m = 10 in 106 runs (earlier insertions not minimal), e.g. core [[0,1,5],[0,4,6],[1,4,7],[2,4,8],[3,4,9],[2,3,4]], profile (0,0,1,1,2,1); found by `src/lb_tree.c`, reproduced by a standalone Python script. Another owner works in every such run, and LB⁺'s rotation repairs r's failure in every run |
| D2.C | Collector theorem: agents with a private good are edges of a multigraph on shared goods; if #E + #pins = #V + 1 and a cover state exists, then with one collector (the only bundle that may exceed two goods) every agent of E is safe | PROVED | `proofs/beta2.md`, `src/test_collector.py`, `results/test_collector.log` | | potential argument: each switch lowers the number of bad agents by one. Random test beyond β = 2: 164,861 multigraphs with pins, 0 failures |
| D2.O | Lemma O: a Q-agent z can hold only its top good while every other agent holds two own goods, whenever the component of a_z in G − z is a tree | PROVED | `proofs/beta2.md` | | Hall's theorem; all bundles ≤ 2 |
| D2 | Conjecture D holds for every connected core with β = 2 (m = 2n − 1), for all n ≥ 2 | PROVED | `proofs/beta2.md`, `src/beta2.py`, `src/verify_beta2.py`, `results/verify_beta2.log` | | constructive, self-contained (re-derives the parts of L4, L5, L8 it uses). Cross-check: the proof run as an algorithm on every β = 2 core and profile, n = 2–8 (98,991,756 core–profile pairs), every output EFX₀ by the raw definition, 0 failures; certificates `results/certs_beta2_{2..8}.json.gz` accepted by `tools/check_certs.py` (`results/check_certs_beta2.log`). Includes n = 2, 3, 4, not covered by R1 |
| D3.M | Multi-collector theorem: the collector theorem D2.C with any number k ≥ 1 of collectors (#E + #pins = #V + k); only one collector receives the extra goods, the others hold their private good alone | PROVED | `proofs/beta3.md` | | Theorem 2 there: the potential argument of D2.C, switching at any collector with a failing walk. Run as an algorithm in `src/beta3.py` (class MultiCollector), cross-checked on every β = 3 core with n ≤ 7 (D3) |
| D3.P | Q-plans suffice: if the orders of a connected core's Q-agents (agents without a private good) admit a Q-plan (roles, spares, dump target; `proofs/beta3.md` §3), the core has an EFX₀ allocation with at most one bundle of more than two goods, for every order of its P-agents; any β | PROVED | `proofs/beta3.md` | | Theorem 5 there: orientations (Lemma 4) in balanced components of K, D3.M in active ones. A Q-plan depends only on the core and the Q-agents' orders |
| D3.0 | Conjecture D holds for every connected core in which every agent has a private good, for every β | PROVED | `proofs/beta3.md` | | Corollary 3 there: the empty Q-plan with β − 1 collectors. Generalizes Case 1 of D2 and L8 |
| D3.2 | Conjecture D holds for every connected core with β = 3 (m = 2n − 2) and at most two Q-agents | PROVED | `proofs/beta3.md`, `src/dg_beta3.py`, `results/hand_plans_beta3.log` | | Lemma 6 with D3.P: explicit Q-plans (a-pins, a b-pin on a shared top, one spare per needy component, one {b, c} 2-holder in one configuration). `dg_beta3.py --hand` builds these plans and both plan checkers accept them on every β = 3 core with q ∈ {1, 2}, 3 ≤ n ≤ 10 (evidence). Covers 143 of the 211 β = 3 cores with n = 6 |
| D3.S | Shortening: every connected β = 3 core reduces to a *reduced* core (≤ 1 P-agent per thread of H, ≤ 2 on a loop at a good) whose Q-plans lift back; reduced β = 3 cores with a Q-agent have n ≤ 10 (for general β ≥ 2, the same argument gives n ≤ 5(β − 1) for reduced cores with a Q-agent; argued only in the prose of `proofs/beta3.md` §7) | PROVED | `proofs/beta3.md` | | Lemmas 8 and 9 there. Reduced β = 3 cores (any q): 2, 15, 45, 96, 108, 81, 36, 11 for n = 3, …, 10, counted by two independent implementations |
| D3.Q | Every connected β = 3 core has a Q-plan for every order of its Q-agents | CERTIFIED | `results/qplans_beta3.json.gz`, `results/qplans_beta3.log`, `results/check_qplans.log`, `src/dg_beta3.py`, `tools/check_qplans.py`, `proofs/beta3.md` | | Lemma 7 there. By D3.S it suffices to check the 394 reduced cores with 3 ≤ n ≤ 10: all 9,160 connected β = 3 cores with 3 ≤ n ≤ 10 enumerated (nauty genbg), list complete by orbit counting (`tools/check_enum.py`, independent of nauty); 110,259 Q-plans, one per reduced core and order of its Q-agents, re-checked by `tools/check_qplans.py` (written without the search code; re-derives reducedness by a different algorithm). q ≤ 2 is also proved by hand (D3.2); q = 3, 4 need all roles, spares and dump targets |
| D3 | Conjecture D holds for every connected core with β = 3 (m = 2n − 2), for all n | CERTIFIED | `proofs/beta3.md`, `results/qplans_beta3.json.gz`, `results/check_qplans.log`, `src/beta3.py`, `src/verify_beta3.py`, `results/verify_beta3.log`, `results/check_certs_beta3.log` | | D3.Q with D3.P; PROVED by hand for q ≤ 2 (D3.2) and for q = 0 at every β (D3.0); the only computation is the finite check of D3.Q. Cross-check: the construction run as an algorithm on every β = 3 core and ranking profile with 3 ≤ n ≤ 7 (161,793,072 core–profile pairs, 27 min on 4 CPUs), every output EFX₀ by the raw definition with at most one large bundle, 0 failures; certificates `results/certs_beta3_{3..7}.json.gz` accepted by `tools/check_certs.py --require-d`. CI re-checks these certificates, their orbit counts, and every Q-plan of D3.Q. Includes n = 3, 4, not covered by R1 |
| D | Every core (nonnegative real additive valuations) has an EFX₀ allocation with at most one bundle of more than two goods | PROVED | `proofs/lb_last_step.md`, `lean/EFX/CorollaryD.lean`, `proofs/real_values.md`, `lean/EFX/RealValues.lean` | `EFX.LB.corollaryD`, `EFX.LB.corollaryD_lists`, `EFX.corollaryD_ordered`, `EFX.corollaryD_of_ordered` | Corollary D of `proofs/lb_last_step.md`, via construction LB⁺ (S2.LB+); two independent reviews found no error. Lean: over any ordered value type (ℝ≥0 by the textbook fact that it satisfies the axioms): `EFX.corollaryD_ordered` (`formal/real-values`, `lean/EFX/RealValues.lean`), derived from `EFX.LB.corollaryD` (over ℕ) via L12 (`EFX.l12`); at V = ℕ it has exactly the statement of `EFX.LB.corollaryD` (`EFX.corollaryD_of_ordered`). Machine-checked in Lean (`formal/lbplus`, PR #18): `EFX.LB.corollaryD` states it in the model's terms (`EFX.Inst`, `numRelevant`, `Inst.EFX0`) for every instance in which every agent values exactly three goods and is balanced (2 v_i(g) ≤ v_i(M) for every good g, i.e. a ≤ b + c), connected or not; every core is such an instance. Earlier partial results: certified for every core with n ≤ 6, connected or not (R3), and for every connected core with n = 7, m ≥ 11 (R2) or n = 8, m ≥ 14 (R4); proved for connected cores with β = 2 (D2), for connected cores in which every agent has a private good (D3.0), and for connected β = 3 cores with at most two Q-agents (D3.2); certified for every connected core with β = 3 (D3); second, independent written proof: `proofs/local_search.md` (Theorem C, local search; row LS3, not formalized) |
| S2.LB | Construction LB (`src/construct.py`) never fails, on every core; with S2.S this implies D | CONJECTURE | | | not needed for D: S2.LB+ (proved, machine-checked) gives D. By Theorem A of `proofs/lb_last_step.md` (S2.R), LB fails only if its run reaches the bad case (written; the link between LB's owner search, `ownerSearch`/`validOwner` in `lean/EFX/LBRun.lean`, and Lemma 1 is not formalized), so S2.LB follows from "LB's lookahead never reaches the bad case". It never does on any core with n ≤ 6, or any connected core with n = 7, 11 ≤ m ≤ 14 or n = 8, m ∈ {15, 16} (`results/lbplus.log`, mode 0: 0 rotations in 1.27 × 10⁹ runs). This covers one labelling per isomorphism class, and LB breaks ties by index, so it is evidence only. S2.LB+ holds for every labelling. Certified, in the labelling stored in the certificates (produced by `cores_nauty.py`), for n ≤ 6 (S2.N6), every connected core with n = 7 (S2.N7) and with n = 8, m ≥ 13 (S2.N8); these supersede #9's uncertified runs (`results/construct_7.log`, `results/construct_8.log`). The open step is the last one: an owner for the overflow bundle that holds no b_k, c_k pair of an agent holding only its top (`proofs/construction.md` §3). Minimality of the last insertion alone is not enough (X5) |
| S2.K | Canonical large bundle: if a core has no EFX₀ allocation with all bundles ≤ 2, it has one whose only large bundle belongs to an agent o in case C and consists of c_o and goods private to agents in case T or B | CONJECTURE | | | true in every C2-failing (core, profile) with n ≤ 6 (full enumeration, `results/large_bundle_structure.log`) and n = 7, m = 13 (SAT with the shape imposed, `results/large_bundle_canon.log`); every witness re-checked against the raw definition. The shape of D2.C's collector, beyond β = 2; `proofs/construction.md` §5.2 |
| T | TARGET: EFX₀ exists whenever every agent has ≤ 3 relevant goods (nonnegative real additive valuations) | PROVED | `proofs/lb_last_step.md`, `proofs/lemmas.md`, `lean/EFX/Target.lean`, `proofs/real_values.md`, `lean/EFX/RealValues.lean` | `EFX.target`, `EFX.target_lists`, `EFX.core_reduction`, `EFX.target_ordered`, `EFX.target_of_ordered` | Corollary T of `proofs/lb_last_step.md`: the CORE theorem of `proofs/lemmas.md` (L3, L2's rules R1 and R2, by induction) with D; two independent reviews found no error. Machine-checked in Lean (`formal/lbplus`, PR #18): `EFX.target` states it in the model's terms (`EFX.Inst`, `numRelevant I i ≤ 3`, `Inst.EFX0`; at least one agent), using only the trusted base of `lean/EFX/Model.lean`. Lean: over any ordered value type (ℝ≥0 by the textbook fact that it satisfies the axioms): `EFX.target_ordered` (`formal/real-values`, `lean/EFX/RealValues.lean`), derived from `EFX.target` (over ℕ) via L12 (`EFX.l12`); at V = ℕ it has exactly the statement of `EFX.target` (`EFX.target_of_ordered`); `EFX.core_reduction` is the CORE theorem (over lists; `EFX.IsCore` is K1–K4 with |A| ≥ 2). Earlier partial results: n ≤ 7 (R1, R5) and the core classes of T2, T3, T4, T5; second, independent written proof: `proofs/local_search.md` (Theorem C, local search; row LS3, not formalized) |
| AUD | Adversarial audit of T and D: an independently written Lean statement of TARGET and of D (bundles as lists `Fin n → List (Fin m)` that partition the goods, a hand-written sum, `X_j ∖ {g}` as `List.erase`, EFX₀ over every good including zero-valued ones) follows from `EFX.target` / `EFX.LB.corollaryD`; all 726 named declarations of the library and their dependencies (4,186) are re-checked by a second, independent kernel (nanoda) | PROVED | `lean/EFX/Audit.lean`, `lean/scripts/audit_kernel.sh`, `results/audit_kernel.log`, `tools/audit_l12.py`, `results/audit_l12.log` | `Audit.target_audit`, `Audit.corollaryD_audit`, `Audit.balanced_iff_pairwise`, `Audit.X1bad_not_EFX0`, `Audit.X1bad_EFX`, `Audit.v2_D_witness`, `Audit.v2_D_violation` | red team (`formal/audit`, PR #19). The statements (`Audit.TargetStmt`, `Audit.DStmt`) were committed before `Model.lean`, `Target.lean` and `CorollaryD.lean` were read (commit order in git history); the bridges needed no change to them. Non-vacuity, by `decide`: an allocation that is EFX but not EFX₀ because of a zero-valued good, failing allocations, instances satisfying the hypotheses of TARGET and of D (one with goods nobody values), `Balanced` equals the pairwise form a ≤ b + c. Kernels (`results/audit_kernel.log`): fresh clone with a clean `.lake` on v4.34.0; `leanchecker --fresh EFX.Target` and `--fresh EFX` pass; nanoda 0.4.19 (Rust, independent of Lean's C++ kernel) checks 4,186 declarations exported by lean4export (all 726 named declarations of the library and their dependencies) admitting only propext, Classical.choice, Quot.sound; both checkers reject two negative controls (`native_decide`, and a proof of 1 = 2 added under `set_option debug.skipKernelTC true`, which `lake build` and `#print axioms` accept, and which `lean/check.sh` accepted until this PR hardened it with a tripwire and `leanchecker --fresh EFX`). L12 read as a referee, no gap found; its table cross-checked on 133,201 profiles (`tools/audit_l12.py`, evidence). Real values are not covered in Lean (L12 is written only) |
| T2 | TARGET holds for every instance whose core has only connected components with β ≤ 2 | PROVED | `proofs/beta2.md` | | D2 with L2, L3, L6, L8 (complete proofs in `proofs/lemmas.md`, Step 0) |
| T3 | TARGET holds for every instance each of whose core components has β ≤ 2, or m ≤ n + 3, or no good relevant to three or more of its agents | PROVED | `proofs/citations.md`, `proofs/beta2.md` | | uses two published theorems, read in full (`proofs/citations.md`): Mahara 2107.09901, Theorem 3 (m ≤ n + 3, monotone valuations, EFX₀ form) and Afshinmehr et al. 2606.18665 (multigraphs, cancelable valuations, EFX₀ form; embedding of private goods in `proofs/citations.md` item 4); with T2 and L6. Existence only: says nothing about D. Left open for TARGET: connected cores with β ≥ 3, m ≥ n + 4 and a good relevant to ≥ 3 agents |
| T4 | TARGET holds for every instance for which the reduction (L2, L3) reaches a core each of whose connected components has β ≤ 3 (m_C ≥ 2n_C − 2) or has no agent without a private good | CERTIFIED | `proofs/beta3.md`, `proofs/beta2.md`, `results/check_qplans.log`, `proofs/min_counterexample.md`, `results/certs_min_cex_9_16.json.gz`, `results/certs_min_cex_10_18.json.gz`, `results/cores_9_16_10_18.json.gz`, `results/min_cex_cores_9_16.log`, `results/min_cex_cores_10_18.log`, `results/check_enum_min_cex.log`, `results/check_certs_min_cex.log`, `src/min_cex_cores.py`, `tools/check_min_cex_cores.py` | | D3, D3.0, D2, L8 with L2, L3, L6 (complete proofs in `proofs/lemmas.md`). PROVED without computation for components with β ≤ 2, with β = 3 and at most two agents without a private good (D3.2), or with no such agent (D3.0); the β = 3 components with three or four such agents rest on the finite check of D3.Q. Combined with T3: every component with β ≤ 3, m ≤ n + 3, no good of degree ≥ 3, or no Q-agent. Independently, for the β ≤ 3 part: minimal-counterexample route (Theorem M5 in `proofs/min_counterexample.md`, with MC3, MC4), which needs no Q-plans and no published theorem: a minimal counterexample within 𝒞_3 is a connected β = 3 core with n ≤ 10 (n ≤ 8 by R1, R2, R4); of the 2,477 + 4,619 connected cores with (n, m) = (9, 16), (10, 18) (complete by orbit counting) exactly 15 + 5 satisfy MC3, and each is certified under all 6^n profiles in model C3 (re-checked without SAT, D shape included) |
| MC1 | Local reductions are sound: if every admissible local state of the smaller instance has an extension (Lemma M1, M1(b) with an unenvied bundle), the configuration does not occur in a minimal counterexample, nor in one within 𝒞_k provided the smaller instance H′ lies in 𝒞_k whenever H does | PROVED | `proofs/min_counterexample.md` | | §1–§2: plain EFX₀ is the inductive statement (closed under sub-instances and gadgets); 𝒞_k = instances whose incidence-graph components have cyclomatic number ≤ k, closed under the reductions used. Checker: `src/reduce.py` |
| MC2 | A minimal counterexample has no agent whose top good is its private good and that shares a good of degree 2 with a P-agent | PROVED | `proofs/min_counterexample.md` | | Lemma M2, hand proof (peel the pair); also certified as families RPT-e/RPT-f of MC3 |
| MC3 | A minimal counterexample (or one within 𝒞_k) has no good of degree 2 valued by two P-agents; every thread carries at most one P-agent | CERTIFIED | `proofs/min_counterexample.md`, `src/reduce.py`, `src/min_cex.py`, `results/min_cex_reductions.json.gz`, `results/min_cex_reductions.log`, `tools/check_reductions.py`, `results/check_reductions.log` | | Theorem M3: configurations pair (36 profiles) and loop (36), each profile reduced by peeling (RPT), deletion (DEL), contraction (CON) or a one-agent gadget (GAD). 183 reductions, 10,066 local states with extensions; re-checked by a second implementation (different state enumeration, L5 case table instead of the raw definition), which also re-derives the coverage and checks that every gadget is a legal reduction (smaller, supported on I′ ∪ ∂, ≤ 3 goods per agent, H′ ∈ 𝒞_k whenever H is); tamper self-test in the log |
| MC4 | A minimal counterexample has n ≤ 5(β − 1) − t, i.e. 5m ≤ 9n − t (t = Σ over goods of degree ≥ 3 of deg − 2) | CERTIFIED | `proofs/min_counterexample.md` | | Corollary M4: counting (proved) on top of MC3. With T3: β + 3 ≤ n ≤ 5β − 6 |
| MC6 | In a minimal counterexample, a P-agent sharing a good of degree 2 with a Q-agent that does not value the P-agent's other shared good does not rank its private good last | CERTIFIED | `proofs/min_counterexample.md`, `results/min_cex_reductions.json.gz`, `results/check_reductions.log` | | Lemma M6 (configuration pq, contraction with an unenvied bundle; 14 of 36 profiles reduce, the 12 claimed re-checked). Not used by T4. Failed reductions for the other 22: `attempts/min-cex-pq-reductions.md` |
| MX.PM | In a core, a singleton state (every agent holds at most one of its goods) that is unitary (nobody prefers a free good to its holding) and has height one (nobody both envies and is envied) is exactly a popular matching of the ranking profile (winners hold their tops, every other agent its best good that is nobody's top); every core in which each good has at most two valuers has one, under every profile | PROVED | `proofs/multigraph_extension.md` | | §1, Lemmas 1.1 and 1.2 (Hall: the reduced graph is a union of paths and cycles). Used instead of the paper's greedy + Reduce Trees. That these states are the paper's simple height-one allocations is an unproved remark: it fails for the core n = 2, m = 3 (two agents sharing three goods), and the paper's objects are undefined for goods with three or more valuers. Popular matchings fail with goods of three valuers: core H3 has none (MX.O) |
| MX.M | Theorem M: every core in which each good has at most two valuers (multigraph core) has an EFX₀ allocation, for every n | PROVED | `proofs/multigraph_extension.md`, `src/mgx.py`, `results/mgx_mg.log` | | new self-contained proof of the part of T3 that uses arXiv 2606.18665: popular matching, the paper's move U1 plus two core moves (Up, R1) with a potential, and a dump lemma. The construction as an algorithm, with every lemma's invariants asserted, gives an EFX₀ output (raw definition) on every profile of every multigraph core with n ≤ 7 (19,354,968 profiles) |
| MX.X | Theorem X: a core with a strict profile that has a popular matching, in which every good with three or more valuers is the top of each of its valuers (class U), has an EFX₀ allocation | PROVED | `proofs/multigraph_extension.md`, `src/mgx.py`, `results/mgx_U.log` | | contains MX.M. Checked as an algorithm on every class-U profile with a popular matching, n ≤ 6 (raw EFX₀ check). Open beyond U: goods with three or more valuers that are someone's b or c (MX.C, and `attempts/multigraph_u1_mixed_top.md`) |
| MX.O | Every core profile has an EFX₀ allocation of the multigraph paper's final shape (height one: no agent both envies and is envied) | REFUTED | `proofs/multigraph_extension.md`, `src/mgx_obstructions.py`, `results/mgx_obstructions.log` | | §5. O1: core H3 (n = 3, two goods with three valuers) has no popular matching, and all six EFX₀ allocations have height two. O2: n = 3, m = 4, a single good with three valuers, a popular matching exists, and all four EFX₀ allocations have height two. Hand proofs, confirmed by brute force over all allocations. So the paper's proof cannot extend without dropping its final shape; MX.X drops it |
| MX.C | In class T (no good with three or more valuers is anyone's top), whenever a popular matching exists, the construction of MX.X ends in a state with an admissible assignment (so an EFX₀ allocation) | CONJECTURE | | | the moves keep all invariants in T (proved, `proofs/multigraph_extension.md` Lemmas 3.1, 3.2); only the dump lemma (4.2) is unproved there. Exhaustive, n ≤ 6: every class-T profile with a popular matching has an admissible assignment, and every output is EFX₀ by the raw definition (`results/mgx_T.log`). Lemma 4.2's case analysis fails first at n = 6, on 194 profiles, where an admissible assignment was found by search (`attempts/multigraph_dump_class_T.md`) |
| T5 | TARGET holds for every instance for which the reduction (L2, L3) reaches a core each of whose connected components has a strict profile consistent with its values that has a popular matching and in which every good with three or more valuers is the top of each of its valuers | PROVED | `proofs/multigraph_extension.md`, `proofs/lemmas.md` | | MX.X with L2, L3, L6 (CORE reduction) and L5(iv) for ties. Contains the multigraph class of T3 (MX.M), without citing the paper. Left open: components with no popular matching, or with a good of three or more valuers that is some valuer's b or c |
| LS1 | In a core, EFX₀ of a partial allocation is ordinal and local: agent i is safe iff every good it envies is unallocated or alone, and, if it holds only its top, its other two goods are not together in a bundle of ≥ 3 goods (Lemma 1). An agent envies only singletons and, holding only its top, the exact bundle of its other two goods (Lemma 2). Threat bound (Lemma 3) | PROVED | `proofs/local_search.md`, `results/ls_lemma1.log` | | Lemma 1 against the raw definition on 281,250 local configurations with three realizations: 0 mismatches (`python src/local_search.py lemma1`). Supporting lemma of LS3, pending review |
| LS2 | Theorem A: in a junk-free EFX₀ partial allocation of a core with an unallocated good, one of four moves keeps EFX₀ and raises the potential: swap for an envied unallocated good, rotation of an envy cycle, filling an empty bundle, or adding a good to an unenvied one-good bundle | PROVED | `proofs/local_search.md`, `results/ls_allstates_2_5.log` | | exhaustive corroboration for n ≤ 5 (junk-free states not settled by these moves: 0). Supporting lemma of LS3, pending review |
| LS3 | Theorem C: Algorithm LS2 computes an EFX₀ allocation with at most one bundle of more than two goods of every core, for any n, connected or not, ties allowed, in polynomial time. Phase 1: Pareto moves on junk-free partial allocations, among them the augmented envy cycle. Phase 2: junk placement from a maximum matching (Hall) | CONJECTURE | `proofs/local_search.md`, `src/ls_alg.c`, `src/local_search.py`, `results/ls_alg_2_6.log`, `results/certs_ls2_2_5.json.gz`, `results/certs_ls2_6.json.gz`, `results/check_certs_ls2.log`, `results/ls_alg_py.log` | | written proof: `proofs/local_search.md` §4 (Theorem C, Claims 1–5; TARGET-strength, also gives D's shape); not claimed until reviewed and formalized. The artifact files are corroboration only: LS2 run on every profile of every connected core with n ≤ 6 (146,640,096 runs; every step and every output checked, outputs by the raw EFX₀ definition and for D's shape); its 372,378 distinct outputs are accepted by `tools/check_certs.py --require-d` for all 3,436 cores; an independent Python implementation from numeric valuations was run exhaustively for n ≤ 5 and on 39,150 random cores with random real values up to n = 100, 0 failures |
| LS4 | One-phase local search (moves fill, swap, rotate, add, single-agent rebundle, champion path; potential = sum of levels, then allocated goods): every partial EFX₀ state of every connected core with n ≤ 5 has an improving move | EVIDENCE | `src/ls_check.c`, `results/ls_allstates_2_5.log` | | exhaustive over all partial states and profiles; one implementation |
| LS5 | The one-phase local search of LS4, with augmented envy cycles added, never gets stuck | REFUTED | `results/ls_stuck6.log`, `src/local_search.py`, `attempts/local-search-one-phase-n6.md`, `results/ls_allstates_core687.log`, `results/ls_reach_core687.log` | | n = 6, m = 9 (core K: agent sets {1,6,7}, {4,5,8}, {0,1,5}, {0,3,6}, {2,3,5}, {2,4,6}): 8 valid improving moves from the empty allocation reach a stuck state. Found by `src/ls_check.c`, replayed by an independent Python implementation from the raw definition. Junk (goods held by agents that do not value them) cannot be relocated; LS2 drops it and places it at the end |
| LS6 | Two-phase search whose Phase 1 has single-agent rebundles and champion paths but no augmented envy cycles always ends in a state that junk placement completes | REFUTED | `results/ls_twophase687_py.log`, `results/ls_twophase_6_m1m2.log`, `attempts/local-search-twophase-m1m2.md` | | n = 6, m = 9 (core K: agent sets {1,6,7}, {4,5,8}, {0,1,5}, {0,3,6}, {2,3,5}, {2,4,6}), 64 profiles, the only failures among all 3,093 n = 6 cores. Checked in Python from the raw definition. With augmented envy cycles no failure remains (`results/ls_twophase_6.log`, n ≤ 6), as Theorem C proves |
| K4.CORE | k = 4 core reduction: rules R1, R2 and L3 reduce TARGET₄ (every agent ≤ 4 relevant goods) to connected *k = 4 cores*: agents with 3 or 4 goods, strictly balanced (top < sum of the others), ≤ 1 private good (3 goods) or ≤ 2 (4 goods; two private goods p, q only with p + q < s + t); every good relevant to someone | PROVED | `k4/SCOUT.md` | `EFX.core_reduction4_conn`, `EFX.core_reduction4`, `EFX.core_reduction4_mixed`, `EFX.target4_of_cores`, `EFX.efx0_split`, `EFX.isCore_of_isCore4`, `EFX.isCore4_of_isCore` | proof in `k4/SCOUT.md` §2 (the CORE induction of `proofs/lemmas.md` never uses k). Lean (`formal/k4-reduction`, `lean/EFX/K4Reduction.lean`, over lists): `EFX.core_reduction4_conn`: if every connected k = 4 core with ≤ N agents has an EFX₀ allocation, so does every instance with ≤ N agents and ≤ 4 relevant goods per agent (`EFX.core_reduction4`: the same for all k = 4 cores); `EFX.IsCore4` is SCOUT's four bullets with |A| ≥ 2: (C1) 3 ≤ |R_i| ≤ 4; (C2) 2·v_i(g) < v_i(M) for every good g; (C3) |P_i| + 2 ≤ |R_i| (P_i = `privateGoods`, relevant to i and to no other agent); (C4) |P_i| = 2 ⟹ v_i(P_i) < v_i(S_i) (S_i = `sharedGoods`, relevant to i and to another agent); (C5) every good relevant to some agent. `EFX.Connected`: every split of the agents into two sides that no good relevant to agents on both sides crosses is trivial; L6 is `EFX.efx0_split`. The Lean proof applies R2 whenever v_i(R_i ∖ P_i) ≤ v_i(P_i) (`EFX.peelR2`; no |P| ≥ 2 needed) and reuses `EFX.junk`, `EFX.peel`, `EFX.peelR2`, `EFX.not_R1` unchanged. `EFX.core_reduction4_mixed`: only connected cores with a 4-good agent are needed (all-3-good k = 4 cores are `EFX.IsCore` cores, `EFX.isCore_of_isCore4`, covered by `EFX.LB.corollaryD_lists`; conversely every `EFX.IsCore` core is a k = 4 core, `EFX.isCore4_of_isCore`); `EFX.target4_of_cores`: the same in the model's terms (`EFX.Inst`, `numRelevant I i ≤ 4`). Reviewed: independent referee, including brute-force tests (the peeling and L3 extensions on 4,000 random instances, 0 failures). New at k = 4: agents with 3 goods remain, and an agent may keep two private goods (merging them is not sound) |
| K4.L | Which of L1–L11 carry over to k ≤ 4: L1, L2, L3, L9, L10 verbatim; L6 with one changed step (a lone agent's d goods would all be private, but at most d − 2 are); L4 becomes m ≤ 2n₃ + 3n₄ ≤ 3n; L7's identity holds but 2n − m can be negative; L5 and L8 fail for 4-good agents (still hold for 3-good agents in k = 4 cores); L8 is replaced by "an agent holding O ⊆ R_i with v(O) ≥ v(R_i ∖ O) is envy-free", which still solves β = 1 cores by orientation; L11 carries over except its last bullet (agents with ≤ 1 private good can be branch vertices, of degree up to 4) | PROVED | `k4/SCOUT.md` | | proofs in `k4/SCOUT.md` §2; L8's and L11's failures have small counterexamples there. Reviewed: independent referee, including brute-force tests (β = 1 orientation on 20,000 cores, 0 failures) |
| K4.OT | Additive order types (weak orders on subsets realized by positive additive values): 4 goods: 1,519 labeled (88 up to relabeling), 336 strict (14 per ranking), 1,271 balanced, 288 balanced strict (12 per ranking, pairwise behaviorally distinct, so EFX₀ in a k = 4 core is not ordinal); 3 goods: 31, 12 strict, 13 balanced, 6 balanced strict; every type has a representative in [1, 16]⁴ (smallest ones: largest value 10) | CERTIFIED | `k4/order_types.py`, `k4/order_types.json`, `results/k4_order_types.log`, `k4/check4.py`, `results/k4_check_selftest.log` | | completeness of the grid by a ray argument: a written proof (`k4/SCOUT.md` §2), reviewed; two computations agree: the grid [1, 16]⁴ and all sums of ≤ 4 of the 101 rays, and [1, 20]⁴ adds nothing; `check4.py` re-enumerates independently (dense ranking of subset sums): 288 and 1,271, 6 and 13 |
| K4.TIE | Ties reduce to strict types: if every strict core profile of a k = 4 core has an EFX₀ allocation, every tied one does | PROVED | `k4/SCOUT.md` | `EFX.tie_reduction`, `EFX.core_reduction4_strict`, `EFX.target4_of_strict_cores` | closure argument with a perturbation supported on R_i, `k4/SCOUT.md` §2. Lean (`formal/k4-reduction`, `lean/EFX/K4Ties.lean`, over ℕ): `EFX.tie_reduction`: if every profile w with the same relevant goods as a connected k = 4 core v that is itself a connected k = 4 core and strict has an EFX₀ allocation, so does v; `EFX.Strict`: two disjoint sets of goods an agent values equally are both worthless to it (⟺ disjoint nonempty S, T ⊆ R_i have v_i(S) ≠ v_i(T)). The ε-perturbation and closedness become the integer perturbation M·v + w, M = 2^|M|, w_i(g) = 2^(goods after g) on R_i (`EFX.tieBreak`); an EFX₀ allocation for it is EFX₀ for v (`EFX.efx0_of_tieBreak`). `EFX.core_reduction4_strict` / `EFX.target4_of_strict_cores` (model): with K4.CORE, TARGET₄ for ≤ N agents follows from EFX₀ for connected strict k = 4 cores with ≤ N agents and a 4-good agent. Scope: the existing certificates discharge this hypothesis for N ≤ 4 (K4.R3 with K4.R4); K4.R5 covers n = 5 only with at most two 4-good agents, which the "every core with ≤ N agents" form cannot use. The discharge also relies on two steps outside Lean: relabeling list cores to the certificates' indices, and K4.OT's grid completeness (every strict type has a representative in the searched grid). Reviewed: independent referee, including brute-force tests (4,000 tied cores, 0 failures); corroborated directly for n ≤ 3 over all 1,271 balanced weak types (`results/k4_certs_2_ties.json.gz`, `results/k4_certs_3_ties.json.gz`, `results/k4_check_3_ties.log`, checked by `k4/check4.py`) |
| K4.R3 | Every connected k = 4 core with n ≤ 3 agents (5 with n = 2, 51 with n = 3; agent degrees 3 and 4), under every strict core profile (agents strictly balanced; p + q < s + t when an agent has two private goods), has an EFX₀ allocation with at most one bundle of more than 2 goods | CERTIFIED | `results/k4_certs_2.json.gz`, `results/k4_certs_3.json.gz`, `results/k4_check_2_3.log`, `k4/check4.py`, `k4/search4.py` | | CEGAR over up to 288³ profiles per core; `check4.py`: independent type enumeration, raw EFX₀, completeness of the core list by orbit counting (labeled counts by a DP, self-tested against brute force), every m in 1..3n (missing m groups rejected), `--expect` core counts, D2 in the exit status, format validation; sensitivity test `k4/test_check4.py` (`results/k4_test_check4.log`) |
| K4.R4 | Every connected k = 4 core with n = 4 (1,002 cores: 135, 309, 339, 219 with one to four 4-good agents), under every strict core profile (agents strictly balanced; p + q < s + t when an agent has two private goods), has an EFX₀ allocation with at most one bundle of more than 2 goods | CERTIFIED | `results/k4_certs_4_n4_1.json.gz`, `results/k4_certs_4_n4_2.json.gz`, `results/k4_certs_4_n4_3.json.gz`, `results/k4_certs_4_pure.json.gz`, `results/k4_check_4_mixed.log`, `results/k4_check_4_pure.log`, `k4/check4.py`, `k4/search4.py` | | pure cores: up to 288⁴ ≈ 6.9·10⁹ profiles each, covered by 22,000 allocations in all; search 6 min on 4 CPUs. Same checker as K4.R3 |
| K4.R5 | Every connected k = 4 core with n = 5 and at most two 4-good agents (1,735 with one, 5,468 with two), under every strict core profile (agents strictly balanced; p + q < s + t when an agent has two private goods), has an EFX₀ allocation with at most one bundle of more than 2 goods | CERTIFIED | `results/k4_certs_5_n4_1.json.gz`, `results/k4_certs_5_n4_2.json.gz`, `results/k4_check_5_n4_1.log`, `results/k4_check_5_n4_2.log`, `k4/check4.py` | | search 10 min on 4 CPUs. Three or four 4-good agents (≈ 14,500 and 13,500 hypergraphs before filtering) and pure cores (4,674; 288⁵ profiles each) not searched |
| K4.R5c | Every connected k = 4 core with n = 5 and exactly three 4-good agents (9,861 cores), under every strict core profile (agents strictly balanced; p + q < s + t when an agent has two private goods), has an EFX₀ allocation with at most one bundle of more than 2 goods | CERTIFIED | `results/k4_certs_5_n4_3.json.gz`, `results/k4_search_5_n4_3.log`, `results/k4_check_5_n4_3.log`, `results/k4_check_plain_5_n4_3.log`, `k4/check4_fast.py`, `k4/check4.py`, `k4/search4.py` | | compute/k4-frontier. `search4.py 5 --n4=3`: 56 min on 3 CPUs, 923,281 allocations, no profile without a D2 allocation. Checker: `k4/check4_fast.py` (the checks of `k4/check4.py`, with a faster coverage step: minimal types, memo, two-level leaves, numpy tables re-checked against the plain loop; its SHA-256 is in the log; tests `results/k4_frontier_test_check4_fast.log`, `results/k4_frontier_test_check4.log`, `results/k4_frontier_oracle_2_3.log`; `k4/frontier/README.md`): ALL OK. Main's unmodified `k4/check4.py` also accepts the file in full: ALL OK (`results/k4_check_plain_5_n4_3.log`; every core also re-covered in the PR #26 review) |
| K4.R5d | Every connected k = 4 core with n = 5 and exactly four 4-good agents (9,846 cores), under every strict core profile (agents strictly balanced; p + q < s + t when an agent has two private goods), has an EFX₀ allocation with at most one bundle of more than 2 goods | CERTIFIED | `results/k4_certs_5_n4_4.json.gz`, `results/k4_search_5_n4_4.log`, `results/k4_check_5_n4_4.log`, `k4/check4_fast.py`, `k4/frontier/search.py` | | compute/k4-frontier. `k4/frontier/search.py 5 --n4=4`: search4.py's cores, types, SAT model and certificate format, with the subsumption scanner `k4/frontier/scan2.c` (`k4/frontier/README.md`); 95 min on 2 CPUs, 1,566,025 allocations, no profile without a D2 allocation. Checker: `k4/check4_fast.py` (the checks of `k4/check4.py`, with a faster coverage step: minimal types, memo, two-level leaves, numpy tables re-checked against the plain loop; its SHA-256 is in the log; tests `results/k4_frontier_test_check4_fast.log`, `results/k4_frontier_test_check4.log`, `results/k4_frontier_oracle_2_3.log`; `k4/frontier/README.md`): ALL OK. Main's coverage loop (`k4/check4.py`) cannot re-check this file in practice: 25–39 s per core, ≈ 70–106 CPU-hours (`results/k4_frontier_plain_timing.log`, PR #26 review); accepted by the reduced loop only (differentially tested against main's loop and brute force, PR #26 review, `results/k4_frontier_test_check4_fast.log`) |
| K4.R5p | Every connected k = 4 core with n = 5 whose agents all have 4 goods (4,674 cores, m = 4–15; up to 288⁵ ≈ 2·10¹² strict profiles each), under every strict core profile (agents strictly balanced; p + q < s + t when an agent has two private goods), has an EFX₀ allocation with at most one bundle of more than 2 goods | CERTIFIED | `results/k4_certs_5_pure.json.gz`, `results/k4_search_5_pure_p0of2.log`, `results/k4_search_5_pure_p1of2.log`, `results/k4_check_5_pure.log`, `k4/check4_fast.py`, `k4/frontier/search.py` | | compute/k4-frontier. `k4/frontier/search.py 5 --pure` in two parts (`--part=0/2`, `--part=1/2`), 75 min each on 2 CPUs, 1,386,132 allocations, no profile without a D2 allocation. No symmetry reduction or type-reducing lemma was needed: the scanner keeps only inclusion-minimal rows and a store of covered prefix sets (`k4/frontier/README.md`). Checker: `k4/check4_fast.py` (the checks of `k4/check4.py`, with a faster coverage step: minimal types, memo, two-level leaves, numpy tables re-checked against the plain loop; its SHA-256 is in the log; tests `results/k4_frontier_test_check4_fast.log`, `results/k4_frontier_test_check4.log`, `results/k4_frontier_oracle_2_3.log`; `k4/frontier/README.md`): ALL OK. Main's coverage loop (`k4/check4.py`) cannot re-check this file in practice: 107 and 561 s on 2 sampled cores, 5 of 5 over 300–600 s in the PR #26 review (`results/k4_frontier_plain_timing.log`); accepted by the reduced loop only (differentially tested against main's loop and brute force, PR #26 review, `results/k4_frontier_test_check4_fast.log`). Supersedes the random evidence K4.E5 |
| K4.R6a | Every connected k = 4 core with n = 6 and exactly one 4-good agent (26,866 cores), under every strict core profile (agents strictly balanced; p + q < s + t when the 4-good agent has two private goods), has an EFX₀ allocation with at most one bundle of more than 2 goods | CERTIFIED | `results/k4_certs_6_n4_1.json.gz`, `results/k4_search_6_n4_1.log`, `results/k4_check_6_n4_1.log`, `results/k4_check_plain_6_n4_1.log`, `k4/check4_fast.py`, `k4/check4.py`, `k4/frontier/search.py` | | compute/k4-frontier. `k4/frontier/search.py 6 --n4=1 --tries=2`: 85 min on 2 CPUs, 2,022,655 allocations, no profile without a D2 allocation. Checker: `k4/check4_fast.py` (the checks of `k4/check4.py`, with a faster coverage step: minimal types, memo, two-level leaves, numpy tables re-checked against the plain loop; its SHA-256 is in the log; tests `results/k4_frontier_test_check4_fast.log`, `results/k4_frontier_test_check4.log`, `results/k4_frontier_oracle_2_3.log`; `k4/frontier/README.md`): ALL OK. Main's unmodified `k4/check4.py` also accepts the file in full: ALL OK (`results/k4_check_plain_6_n4_1.log`; reproduced in the PR #26 review, 22 min 56 s on 2 CPUs). Not searched: n = 6 with two or more 4-good agents (119,283 cores with two; genbg graphs before the private-goods filter: 183,524, 445,606, 663,960, 564,765 and 218,093 with 2, 3, 4, 5, 6 of them; `results/k4_frontier_counts_6.log`) |
| K4.E5 | Pure n = 5 cores: D2-shaped EFX₀ allocations exist | EVIDENCE | `k4/sample5.py`, `results/k4_sample_5_pure.log` | | all 4,674 pure cores, 30 random strict profiles each (140,220): no failure. Random only; superseded by K4.R5p (exhaustive, CERTIFIED) |
| K4.C3 | In every k = 4 core some EFX₀ allocation has all bundles ≤ 3 goods | REFUTED | `results/k4_structure_3.log`, `k4/small_claims.json`, `k4/verify_small.py`, `results/k4_verify_small.log` | | n = 3: fails in 10 of 51 cores (smallest m = 6); all bundles ≤ 2 fails already at n = 2 (two identical agents, type (8, 4, 3, 2)). Every failure instance is confirmed by `verify_small.py` (brute force over all n^m allocations, explicit integers, raw definition, no SAT) |
| K4.LB | With at most one bundle of more than 2 goods, that bundle can always be kept at the counting bound max(3, m − 2n + 2) | REFUTED | `results/k4_structure_3.log`, `k4/small_claims.json`, `k4/verify_small.py`, `results/k4_verify_small.log` | | n = 3: above the bound in 13 of 51 cores; smallest: m = 6 needs 4 (bound 3); m = 8 needs 6 = 2n (bound 4): agents {0,2,6,7}, {1,4,6,7}, {3,5,6,7}, values (4,3,8,2), (4,1,6,8), (4,1,6,8), whose only EFX₀ allocations pool all six private goods (brute force). Exact least sizes per core (the table in `k4/SCOUT.md` §4) are from one implementation |
| K4.SDJ | Serial dictatorship plus junk (the shape of LB's output) suffices at k = 4: for some agent order, the serial-dictatorship picks plus a placement of the unpicked goods with at most one bundle of more than 2 goods is EFX₀ | EVIDENCE | `k4/sdj.py`, `results/k4_sdj_sample_2_3.log`, `results/k4_sdj_sample_4_pure.log` | | exhaustive for n = 2 (CEGAR); n = 3: all 102,000 random strict profiles (2,000 per core); n = 4 (pure): all 657,000 random profiles (3,000 per core, `results/k4_sdj_sample_4_pure.log`). The test detects failures (reproduces the 14 of 216 size-≤ 2 failures on the k = 3 core H3) |
| K4.D | Conjecture D for k ≤ 4: every k = 4 core has an EFX₀ allocation with at most one bundle of more than two goods (with K4.CORE and L6 this implies TARGET₄) | CONJECTURE | `k4/SCOUT.md` | | certified for every k = 4 core with n ≤ 5 that has a 4-good agent (K4.R3–K4.R5, K4.R5c, K4.R5d, K4.R5p: 32,642 cores; all-3-good cores are k = 3 cores: row D) and with n = 6 and exactly one 4-good agent (K4.R6a: 26,866 cores); no failing profile found anywhere (also random and hill-climbing search on 130 cores with n = 6–8, `results/k4_hunt_*.log`, EVIDENCE); route: LB₄ (`k4/SCOUT.md` §5) |
| K4.T | TARGET₄: EFX₀ exists whenever every agent has ≤ 4 relevant goods (nonnegative additive valuations) | CONJECTURE | `k4/SCOUT.md` | | open in the papers read (`k4/SCOUT.md` §1: Viswanathan–Mehta claim ordinary EFX only, with a proof sketch); holds whenever the reduction reaches core components with at most 5 agents, or 6 agents at most one of which has 4 goods (K4.R3–K4.R5, K4.R5c, K4.R5d, K4.R5p, K4.R6a with K4.CORE, K4.OT, K4.TIE, L6, and TARGET for all-3-good components) |
| K4.MC0 | A minimal counterexample to TARGET₄ (fewest agents, then goods; or one within 𝒞_β, the instances whose components have cyclomatic number ≤ β) is a connected k = 4 core, may be taken with strict types (only the types matter), has an agent with 4 goods and n ≥ 5, and has at least three 4-good agents if n = 5 | CERTIFIED | `k4/MINCEX.md` | | `k4/MINCEX.md` §1: (a)–(c) proved (K4.CORE, L6 of K4.L, K4.TIE, TARGET); (d) rests on the certified K4.R3–K4.R5. Reviewed: two independent reviews of PR #23 (brute-force referee check of every stored extension on ~5,200 concrete instances, 0 violations; exact computational re-run) |
| K4.MC1 | Local reductions are sound at k = 4: Lemmas M1 and M1(b) of `proofs/min_counterexample.md` hold verbatim for TARGET₄ with gadget agents of ≤ 4 goods (the gadget may depend on the profile; a boundary good need not have an outside valuer) | PROVED | `k4/MINCEX.md` | | `k4/MINCEX.md` §2. Reviewed: two independent reviews of PR #23 (brute-force referee check of every stored extension on ~5,200 concrete instances, 0 violations; exact computational re-run) |
| K4.MC2 | A minimal counterexample to TARGET₄ (or one within 𝒞_β) has no agent with two private goods | CERTIFIED | `k4/MINCEX.md`, `k4/reduce4.py`, `k4/mincex4.py`, `results/k4_min_cex_reductions.json.gz`, `results/k4_min_cex_reductions.log`, `k4/check_reductions4.py`, `results/k4_check_min_cex_reductions.log` | | configuration single-PP4: all 144 types reduced by one-agent gadgets with one private good fewer (5 gadgets); re-checked by the independent `check_reductions4.py` (own types, states, raw definition, domination, gadget legality, hard-coded configurations; sensitivity test in its log). Reviewed: two independent reviews of PR #23 (brute-force referee check of every stored extension on ~5,200 concrete instances, 0 violations; exact computational re-run) |
| K4.MC3 | In a minimal counterexample to TARGET₄ (or one within 𝒞_β) no good of degree 2 is shared by two P3 agents (3 goods, one private) | CERTIFIED | `k4/MINCEX.md`, `k4/reduce4.py`, `k4/mincex4.py`, `results/k4_min_cex_reductions.json.gz`, `results/k4_min_cex_reductions.log`, `k4/check_reductions4.py`, `results/k4_check_min_cex_reductions.log` | | MC3's configurations pair and loop (36 + 36 rankings) re-certified with the k = 4 tools, same counts per reduction as `src/reduce.py`. Reviewed: two independent reviews of PR #23 (brute-force referee check of every stored extension on ~5,200 concrete instances, 0 violations; exact computational re-run) |
| K4.MC5 | In a minimal counterexample to TARGET₄ (or one within 𝒞_β) with strict types (K4.MC0), if a P3 agent f shares a good g of degree 2 with a Q3 or P4 agent e, then e does not value f's other shared good y, f ranks y > p_f > g, and e ranks g first; with a Q4 agent, the profile of (e, f) lies, under every labeling of e's other goods, in a recorded set (1,080 of 1,728 open, 72 closed) | CERTIFIED | `k4/MINCEX.md`, `k4/reduce4.py`, `k4/mincex4.py`, `results/k4_min_cex_reductions.json.gz`, `results/k4_min_cex_reductions.log`, `k4/check_reductions4.py`, `results/k4_check_min_cex_reductions.log`, `results/k4_min_cex_px_uncovered.json`, `attempts/k4-mincex-px-open.md` | | configuration px, DEL and one-agent gadgets from a menu of 1,665 valuation classes; reduced Q3 34/36 (closed 36/36), P4 1,718/1,728 (closed all), Q4 648/1,728 (closed 1,656); the profiles no reduction covers are re-derived by the checker (file SHA-256 in its log). Reviewed: two independent reviews of PR #23 (brute-force referee check of every stored extension on ~5,200 concrete instances, 0 violations; exact computational re-run) |
| K4.MC4 | A minimal counterexample to TARGET₄ (or one within 𝒞_β) has n ≤ 3(β − 1) agents; from K4.MC2 and K4.MC3 alone, n ≤ 5(β − 1) − t − 2n_Q4 | CERTIFIED | `k4/MINCEX.md` | | counting (`k4/MINCEX.md` §5) on top of K4.MC2, K4.MC3, K4.MC5, as for k = 3's MC4; re-derived by hand by the referee and confirmed by independent genbg enumerations (β = 3: n = 5..10; β = 4: n = 9..11) and by `results/k4_min_cex_shapes_3.log` (β = 3 up to n = 10) |
| K4.MC6 | TARGET₄ holds for every instance whose k = 4 core components have cyclomatic number β ≤ 3 (every instance in 𝒞₃ with ≤ 4 relevant goods per agent has an EFX₀ allocation) | CERTIFIED | `k4/MINCEX.md`, `k4/mincex_shapes.py`, `k4/mincex_cert.py`, `results/k4_min_cex_shapes_3.json.gz`, `results/k4_min_cex_shapes_3.log`, `results/k4_min_cex_cores_3.json.gz`, `results/k4_min_cex_cores_3.log`, `k4/check_mincex_cores.py`, `results/k4_check_min_cex_cores_3.log`, `k4/test_check_mincex_cores.py`, `results/k4_test_check_mincex_cores.log` | | β = 0 impossible (Γ′ has minimum degree ≥ 2), β = 1 by K4.L, β = 2 by K4.MC4 and K4.MC0(d); β = 3 leaves 9 cores (n = 5, 6; 21.7 million restricted profiles), all certified in the D2 shape (D2 in the checker's exit status). Independent check: re-enumerates the 82,782 connected incidence graphs with 5 ≤ n ≤ 6, β = 3 (complete by orbit counting against `k4/check4.py`'s DP), finds the same 9, and verifies the uncovered-profile file's SHA-256. Uses K4.CORE, K4.TIE, K4.L, K4.MC0–K4.MC5, K4.R3–K4.R5, TARGET; no published theorem. Reviewed: two independent reviews of PR #23 (brute-force referee check of every stored extension on ~5,200 concrete instances, 0 violations; exact computational re-run) |
| K4.MC7 | TARGET₄ holds for every instance whose k = 4 core components have cyclomatic number β ≤ 4 (every instance in 𝒞₄ with ≤ 4 relevant goods per agent has an EFX₀ allocation), using the multigraph theorem of Afshinmehr et al. for 6 graphical cores | CERTIFIED | `k4/MINCEX.md`, `k4/gamma_orbits.py`, `results/k4_gamma_orbits_4.log`, `k4/mincex_shapes.py`, `results/k4_min_cex_shapes_4.json.gz`, `results/k4_min_cex_shapes_4.log`, `k4/mincex_cert.py`, `k4/mincex_ckpt.py`, `results/k4_min_cex_cores_4.json.gz`, `results/k4_min_cex_cores_4.log`, `k4/check_mincex_cores4.py`, `results/k4_check_min_cex_cores_4.log`, `k4/test_check_mincex_cores4.py`, `results/k4_test_check_mincex_cores4.log`, `k4/check4.py`, `k4/check_reductions4.py`, `k4/check_mincex_cores.py`, `results/k4_min_cex_px_uncovered.json`, `results/k4_check_min_cex_reductions.log`, `proofs/citations.md`, `proofs/multigraph_digest.md` | | `k4/MINCEX.md` §8. Reviewed: two independent reviews of PR #28 (mathematical referee; computational re-run, 32,800 own random profiles, 0 uncovered). β = 4 leaves 5,558 cores (5 ≤ n ≤ 9; Γ′ lists complete by orbit counting, 111,078 graphs); 5,552 certified in the D2 shape (search4.py's CEGAR, with compute/k4-frontier's scan2 scanner for the hard ones); the independent Γ′-based checker re-derives the same 5,558 (--expect flags on the total, the split by n and the 6 graphical cores) and checks coverage with check4.py's C search. Uses the multigraph theorem of Afshinmehr et al. (arXiv 2606.18665, read in full; EFX₀ form; cancelable valuations ⊇ additive with zeros; private goods embedded as edges to another agent, our inference, `proofs/citations.md` item 4, as in T3) for the 6 graphical all-P4 cores (n = 6, m = 15; 288⁶ profiles each), the only candidates not certified; certifying them directly would make K4.MC7 literature-free. Uses K4.MC0–K4.MC6, K4.CORE, K4.TIE, K4.L, K4.R3–K4.R5, TARGET |
| K4.LS.SOUND | Algorithm LS4 for k ≤ 4 (`k4/local_search4.md` §2: Phase 1 on junk-free EFX₀ partial allocations with single-agent rebundles M1, envy-cycle rotations R and exchange cycles X in which agents may keep part of their own bundle; Phase 2 an empty-bundle dump, a single dump, or a dump plus solo goods) is sound: every state is a junk-free EFX₀ partial allocation, there are at most Σ_i (2^{d_i} − 1) ≤ 15n Phase-1 moves, every output is a complete EFX₀ allocation, and it can fail only at a state that no M1, R or X move improves and no Phase-2 placement completes | CONJECTURE | `k4/local_search4.md` | | Theorem 1 with Lemmas A, B and 2 (the empty-bundle dump is valid once no M1 move applies; uses d_i ≤ 4). Written proof, pending independent review; new K4.LS rows stay CONJECTURE or EVIDENCE until reviewed. Polynomial: the number of moves only; the implementation finds exchange cycles and the Phase-2 split by enumeration |
| K4.LS.DEAD | Dead end for two-phase Pareto local search at k = 4: in the pure core n = 4, m = 7 with agent sets {0,2,5,6}, {0,3,4,6}, {1,2,4,6}, {1,3,5,6} and values (8,10,6,3), (5,2,4,8), (1,8,6,4), (2,3,6,10), the junk-free EFX₀ partial allocation with bundles {2}, {6}, {1,4}, {3,5} and pool {0} is reached from the empty allocation by 15 single-agent Pareto rebundles; no M1, R, exchange-cycle or coalition move improves it, and no complete EFX₀ allocation gives every agent at least its value in it. So conjecture TP₄ (`k4/local_search4.md` §4) is false, and no two-phase local search whose Phase 1 makes Pareto improvements with arbitrary choices (as Theorem C, row LS3, allows at k = 3) and whose Pareto move set contains single-agent rebundles proves TARGET₄ | EVIDENCE | `attempts/k4-ls-dead-end.md`, `k4/local_search4.md`, `k4/ls4_attempts.py`, `results/k4_ls4_attempts.log`, `k4/ls4alg.c`, `k4/ls4_deadend.c`, `results/k4_ls4_deadend_2.log`, `results/k4_ls4_deadend_3_sample.log`, `results/k4_ls4_deadend_4_m6_sample.log`, `results/k4_ls4_deadend_4_sample.log`, `results/k4_ls4_deadend_4_m7_sample.log` | | a refutation in substance (Proposition 7), confirmed by two independent computations (the C trace, and a plain-Python brute force over all 4⁷ allocations from the raw definition); EVIDENCE under the claim policy for new rows until reviewed. Where dead ends occur (any junk-free EFX₀ partial allocation that no complete EFX₀ allocation weakly dominates): none at n = 2 (exhaustive), none in 1,020,000 sampled n = 3 profiles, none in 424,000 sampled n = 4 profiles with m ≤ 6; at m = 7, 34 of 576,000 sampled profiles have one (20 of the 288 cores, with two, three or four 4-good agents), each reachable by single-agent rebundles |
| K4.LS.RUN | Algorithm LS4 with its default choice rule (`k4/ls4alg.c`) outputs a complete EFX₀ allocation (raw definition) on every strict profile of every k = 4 core with n ≤ 3 and on every tied profile (each checked through its perturbation class), and on every profile of the 444 n = 4 cores with one or two 4-good agents; its failures are rare elsewhere | EVIDENCE | `k4/ls4alg.c`, `k4/ls4alg_run.py`, `k4/ls4_allstates.c`, `results/k4_ls4_2_ties.log`, `results/k4_ls4_3_ties.log`, `results/k4_ls4_4_n4_1.log`, `results/k4_ls4_4_n4_2.log`, `results/k4_ls4_4_sample.log`, `results/k4_ls4_5_n4_2_sample.log`, `results/k4_ls4_5_n4_1_sample.log`, `results/k4_ls4_4_pure_alt_sample.log`, `results/k4_ls4_allstates_2.log`, `results/k4_ls4_allstates_3_sample.log`, `results/k4_ls4_sensitivity.log` | | exhaustive: n = 2 (189,216 strict profiles), n = 3 (299,837,376 strict; 24,690,461,987 tied), n = 4 with one 4-good agent (7,247,232) and with two (724,847,616) — 0 failures. Sampled: n = 4 with three 4-good agents (33,900,000), n = 5 with two (54,680,000), n = 5 with one (34,700,000) — 0 failures; pure n = 4 (21,900,000): 20 failures in 10 cores with m = 7, 8, 9 (4, 5 and 1 cores), stable states where LS4 stops without a placement; 8 of the 19 logged are dead ends (K4.LS.DEAD), 11 admit coalition moves; the alternative rule `-DALT` fails on 23 of the same 21,900,000. Every stable state (not only those reached) can be completed: n = 2 exhaustive (24,508 states), n = 3 on 510,000 sampled profiles (191,274 states). Sensitivity test: without exchange cycles, or with an unchecked dump, the runs fail |
| K4.LS.K3 | TP₄ holds for every core whose agents all have three goods | CONJECTURE | `k4/local_search4.md` | | Proposition 3: every LS2 step is an M1, R or X move of LS4, and LS2's Phase-2 placement has LS4's dump-plus-solo shape, so it follows from Claims 2–4 of Theorem C (`proofs/local_search.md`, row LS3), itself pending review |
| K4.LS.ONE | TP₄ holds for every stable state with exactly one source: there the single dump applies. More generally, if the single dump fails at a source s, the agent envying a smallest subset of Y_s ∪ U takes a threat-free set meeting Y_s and U and is not reachable from s (else an exchange cycle applies) | CONJECTURE | `k4/local_search4.md` | | Lemma 5 and Proposition 4 (the k = 4 form of LS2's champion step 6 and Claim 2 (h)); any instance with d_i ≤ 4. Written proof, pending review |
| K4.LS.VAR | Weaker variants of LS4 fail: Phase 1 without exchange cycles (n = 2, m = 4); exchange cycles that cannot keep own goods, which include the k = 3 champion cycles (n = 3, m = 6); exchange cycles of length ≤ 2 (n = 3, m = 5); exchange cycles with at most one pool-using agent (n = 5, m = 8); a Phase 2 limited to one dump even after all coalition Pareto moves (n = 4, m = 7: the only EFX₀ completion that is at least as good for everyone splits the pool); a value-free "clean" Phase 2 (n = 3, m = 5); the simplest polynomial split (n = 4, m = 6); an exact-search Phase 2 in place of dump-plus-solo (the dead end of K4.LS.DEAD); the alternative choice rule `-DALT` (n = 4, m = 7) | EVIDENCE | `attempts/k4-ls-no-exchange.md`, `attempts/k4-ls-exchange-no-keep.md`, `attempts/k4-ls-short-cycles.md`, `attempts/k4-ls-one-pool-agent.md`, `attempts/k4-ls-single-dump.md`, `attempts/k4-ls-clean-placement.md`, `attempts/k4-ls-simple-split.md`, `attempts/k4-ls-exact-placement.md`, `attempts/k4-ls-alt-rule.md`, `k4/ls4_attempts.py`, `results/k4_ls4_attempts.log`, `k4/ls4.c`, `k4/ls4_run.py` | | each configuration (smallest found, not proved smallest) replayed by an independent brute force from the raw definition (`k4/ls4_attempts.py`); how often each variant fails comes from one implementation (`k4/ls4.c`, or `k4/ls4alg.c` for `-DALT`) on random samples |
| K4.LSP.SOUND | Algorithm LS4⁺_k (LS4 plus, when stuck, coalition re-divisions C_k: at most k agents re-divide their bundles and the pool so that the level sum rises, some may lose; `k4/ls4plus.md` §2) is sound and makes at most Σ_i (2^{d_i} − 1) ≤ 15n moves; LS4⁺_n fails only at a junk-free EFX₀ partial allocation that maximizes the level sum and admits no placement | CONJECTURE | `k4/ls4plus.md` | | Theorem 1⁺, written proof pending review. Only the number of moves is polynomial; C_n is a search over all junk-free partial allocations |
| K4.LSP.GM | Conjecture GM₄: in every k = 4 core, for every strict profile, every junk-free EFX₀ partial allocation that maximizes the level sum among all junk-free EFX₀ partial allocations of that strict profile admits a placement of its pool (LS4's Phase-2 shapes; at a maximum (a)–(c) are special cases of (d)). Equivalently, no dead end maximizes the level sum. It implies that LS4⁺_n never fails, hence (with K4.TIE and K4.CORE) TARGET₄ | REFUTED | `k4/ls4plus.md`, `k4/ls4_gm.c`, `k4/ls4alg_run.py`, `results/k4_gm_2.log`, `results/k4_gm_3_sample.log`, `results/k4_gm_4_mixed_sample.log`, `results/k4_gm_4_pure_sample.log`, `results/k4_lsp_variants_19.log`, `results/k4_gm_pot2_2.log`, `results/k4_gm_pot2_3_sample.log`, `k4/gm4.md`, `k4/gm4_counterexample.py`, `results/k4_gm4_counterexample.log` | | every maximal state checked, not only reached ones, and every placement re-checked independently (raw EFX₀): n = 2 exhaustive (189,216 profiles, 236,176 maximal states), n = 3 (1,020,000 random profiles), n = 4 with one to three 4-good agents (1,566,000), pure n = 4 (4,380,000 random profiles, 6,226,242 maximal states), 0 failures. Only maxima with a nonempty pool test GM₄: 502,281 of them (1–8% of the maxima); in these runs all were placed by an empty-bundle dump or a single dump. Refuted in #30 (row K4.GM.CEX): in the n = 4, m = 7 core of instance E (two 4-good agents), a maximum is a dead end. Confirmed by the independent brute force `k4/gm4_counterexample.py` (instances A–F; log `results/k4_gm4_counterexample.log`); the coordinator and #29 report re-checks of instance A (not recorded in this repository). The existence form is refuted too (K4.GM.E). Holds for three-good cores given K4.LS.K3 and the proof of LS3; a counterexample has at least two sources and no empty bundle (`k4/local_search4.md` Lemma 2, Proposition 4; rests on K4.LS.SOUND and K4.LS.ONE, pending review). The fixed-priority variant had no failure on the 19 logged LS4 failure profiles, at n = 2 (exhaustive) or on 102,000 random n = 3 profiles, the only profiles tested here, but it fails for some agent orders (K4.GM.VAR (e), instance Q); leximin fails (K4.LSP.VAR). Equivalence proof (from the referee of PR #29): `k4/ls4plus.md` §2 |
| K4.LSP.RUN | LS4⁺_n (`k4/ls4alg.c -DCMOVE=8`) outputs a complete EFX₀ allocation (raw definition) on the profiles where LS4 fails and on LS4's regression classes | EVIDENCE | `k4/ls4alg.c`, `results/k4_lsp_4_pure_sample.log`, `results/k4_lsp_4_n4_1.log`, `results/k4_lsp_4_n4_3_sample.log`, `results/k4_lsp_3_ties.log`, `results/k4_lsp_2_ties.log`, `results/k4_lsp_5_n4_2_sample.log` | | 0 failures: the same 21,900,000 random pure n = 4 profiles on which LS4 fails 20 times; n = 4 with one 4-good agent (exhaustive, 7,247,232); with three (33,900,000 random); n = 2 and n = 3 exhaustive with ties; n = 5 with two 4-good agents (54,680,000 random). On the pure n = 4 sample the coalition move was used exactly 20 times (14 by 2 agents, 4 by 3, 2 by all 4), once per LS4 failure |
| K4.LSP.VAR | LS4⁺ designs that fail: stopping at the first placement (n = 4, m = 7), coalition re-divisions of at most 2 or 3 agents (n = 4, m = 7: only a re-division by all 4 agents raises the level sum), and leximin instead of the level sum (the dead end of K4.LS.DEAD is its own unique leximin maximum) | EVIDENCE | `attempts/k4-lsp-early-stop.md`, `attempts/k4-lsp-bounded-coalitions.md`, `attempts/k4-lsp-leximin.md`, `k4/lsp_attempts.py`, `results/k4_lsp_attempts.log`, `results/k4_ls4_failures_4_pure.tsv`, `k4/lsp_escape.py`, `results/k4_lsp_escape.log`, `k4/lsp_variants.py`, `results/k4_lsp_variants_19.log`, `results/k4_ls4_4_pure_20k.log`, `results/k4_lsp_4_pure_20k_c2.log`, `results/k4_lsp_4_pure_20k_c3.log`, `results/k4_gm_pot1_2.log`, `results/k4_gm_pot1_3_sample.log` | | configurations (smallest found) replayed by an independent brute force (`k4/lsp_attempts.py`); failure counts from `k4/ls4alg.c` and `k4/ls4_gm.c` (`k4/lsp_variants.py` on the 19 logged failures, and the sample logs). Escape study of the 19 logged LS4 failures (`k4/ls4plus.md` §1) replayed in plain Python by `k4/lsp_escape.py` |
| K4.LB4.S | Soundness and structure of LB₄'s pre-allocations (`k4/lb4.md` §1–§2), for any k: every completion of a valid pre-allocation (bases and needs of any size) that satisfies (OC₄), with the owner's needs from its base or from its bundle, is EFX₀, and every bundle but the owner's has at most 2 goods (Theorem 1′₄; so at most one bundle has more than 2 goods); the counting (#F = #NA, and ω equals the number of goods needed alone minus σ, with cap = 2 − base size counted with its sign; if ω ≤ 0 and every base has at most 2 goods, a completion without owner exists; with the other slots filled the owner holds ω + 2 goods); self-protection (Lemma 2₄) for agents with at most 4 relevant goods whose base is a pick (with 5 relevant goods it fails: an instance of every other hypothesis in which the agent is threatened); Lemma 3₄: if some completion with owner o satisfies (OC₄), one does with at least min(#J, s₀) slot goods, for a strictly balanced owner with at most 4 relevant goods, its base among them, that is free with ω ≥ 1 (the other bases having at most 2 goods) or has a base of at least 3 goods (so the owner search loses nothing by trying only those sizes), except for a rotated owner with a one-good base | PROVED | `k4/lb4.md`, `lean/EFX/PreAllocK.lean` | `EFX.LB4.Valid.sound`, `EFX.LB4.Valid.sound_ownerNeeds`, `EFX.LB4.efx0_of_needs`, `EFX.LB4.Completion.length_le_two`, `EFX.LB4.Completion.frozen_base`, `EFX.LB4.sound_model`, `EFX.LB4.Needs.pick`, `EFX.LB4.numFrozen_eq`, `EFX.LB4.omega_eq`, `EFX.LB4.complete_none_exists`, `EFX.LB4.Completion.owner_length`, `EFX.LB4.selfProtect`, `EFX.LB4.selfProtect_seq`, `EFX.LB4.selfProtect_core`, `EFX.LB4.selfProtect_five`, `EFX.LB4.Ex5.counterexample`, `EFX.LB4.ownerSearch_exact_base`, `EFX.LB4.ownerSearch_exact`, `EFX.LB4.SoundCompletion.efx0_d2`, `EFX.LB4.d2_shape`, `EFX.LB4.k4D_of_completion`, `EFX.LB4.target4_of_completions`, `EFX.LB4.Ex.sound`, `EFX.LB4.ExB.sound` | machine-checked in Lean (`formal/k4-prealloc`, PR #31), stated over the Lean definitions of pre-allocations, needs, validity, completions and (OC₄) in `lean/EFX/PreAllocK.lean`, which a reader must check against §1 (the correspondence is in `lean/README.md`). Theorem 1′₄ for any additive values in ℕ (rationals by scaling; real values not formalized at k = 4), no bound on relevant goods; the text's hypotheses "only X_o may have more than 2 goods" and "frozen agents hold exactly their base" hold in every completion (`EFX.LB4.Completion.length_le_two`, `EFX.LB4.Completion.frozen_base`), so they are not assumed; with the owner's needs from its bundle the owner's base needs are not used. Lemma 2₄ is stated with the hypothesis that the agent has at most 4 relevant goods, and shows more: the agent does not envy the owner's bundle; `EFX.LB4.selfProtect_seq` covers slots filled one agent at a time, whatever else is placed; with 5 relevant goods the value argument fails (`EFX.LB4.selfProtect_five`), and `EFX.LB4.Ex5.counterexample` (from the independent audit of PR #31) is an instance of every other hypothesis of `EFX.LB4.selfProtect` in which the agent is threatened, both by `decide`. Lemma 3₄: `EFX.LB4.ownerSearch_exact_base` with the text's caps (a base of three or more goods, or a free owner with ω ≥ 1 and the other bases of at most 2 goods); `EFX.LB4.ownerSearch_exact` for any slot count up to the other agents' slots, which covers `k4/lb4.c`'s s₀ (a rotated agent gets no slot there). Non-vacuity, by `decide`: `EFX.LB4.Ex.sound` (an owner's bundle of 3 goods; valid only with the owner's needs from its bundle) and `EFX.LB4.ExB.sound` (`EFX.LB4.Valid.sound`'s hypotheses with the owner's needs from its base, a frozen agent, a two-good base, a filled slot and a 3-good owner's bundle). Independent audit of PR #31 (coordinator): statements faithful to §1–§2, `lean/check.sh` and `leanchecker --fresh` pass, Theorem 1′₄ restated from the model's primitives and derived from `EFX.LB4.sound_model`. The chain to K4.D: a sound completion of an instance gives the conclusion of K4.D for it (`EFX.LB4.k4D_of_completion`), and sound completions of every connected strict k = 4 core with a 4-good agent give TARGET₄ (`EFX.LB4.target4_of_completions`, with K4.CORE and K4.TIE). Not formalized: LB₄ itself and its owner step's test for one set C (that LB₄'s outputs are sound completions is K4.LB4.SH). Written proofs: the referee (coordinator's batch 2) found Theorem 1′₄, Lemma 2₄ and the counting correct; the k = 3 case of Theorem 1′₄ is Theorem 1′ of `proofs/lb_last_step.md` (PROVED) |
| K4.LB4.SH | Every allocation LB₄ returns (`k4/lb4.md` §2) is a completion satisfying (OC₄) of a valid pre-allocation whose bases other than the owner's have at most 2 goods, with the owner's needs from its bundle (§2, Shape); hence, by K4.LB4.S, it is EFX₀ with at most one bundle of more than 2 goods | CONJECTURE | `k4/lb4.md` | | written argument by inspection of LB₄'s definition (Shape paragraph), added after the referee's review, awaiting review; LB₄ is not defined in Lean. Split from K4.LB4.S when that row's other content was machine-checked (PR #31); the step from a sound completion to EFX₀ with at most one large bundle is K4.LB4.S. Every output of `k4/lb4.c` is checked against the raw EFX₀ definition: 0 failures |
| K4.LB4 | Construction LB₄ (`k4/lb4.md` §2: serial dictatorship with priority to agents that lost a good, every insertion sequence, need-shrinking upgrades, an owner search (exact by Lemma 3₄, except for a rotated owner with a one-good base) with the owner's needs from its bundle, one rotation along a need chain that may end at an upgraded agent) never fails on any strict profile of any k = 4 core. With K4.LB4.S and K4.LB4.SH (outputs EFX₀ with at most one large bundle) and K4.TIE (a strict perturbation keeps the allocation) it implies K4.D | CONJECTURE | `k4/lb4.md` | | evidence in K4.LB4.E. A proof needs analogues of Theorems A and B of `proofs/lb_last_step.md` and a choice of insertion sequence; what breaks is listed in `k4/lb4.md` §5 |
| K4.LB4.E | LB₄ fails on no strict profile of any core with n ≤ 4 (1,058 cores) or with n = 5 and at most two 4-good agents (7,203), nor on any tied profile (all balanced weak types) with n ≤ 3 | EVIDENCE | `k4/lb4.c`, `k4/lb4_run.py`, `results/k4_lb4_run_2_3_4mixed.log`, `results/k4_lb4_run_4pure_5.log`, `results/k4_lb4_certs_2.json.gz`, `results/k4_lb4_certs_3.json.gz`, `results/k4_lb4_certs_2_ties.json.gz`, `results/k4_lb4_certs_3_ties.json.gz`, `results/k4_lb4_certs_4_n4_1.json.gz`, `results/k4_lb4_certs_4_n4_2.json.gz`, `results/k4_lb4_certs_4_n4_3.json.gz`, `results/k4_lb4_check.log`, `k4/check4.py`, `k4/test_lb4.py`, `results/k4_lb4_test.log` | | exhaustive (lazy type branching: leaf weights add up to every profile; equal to brute force on n ≤ 3); every output checked against the raw definition with integer values; LB₄'s outputs for n ≤ 3 (strict, ties) and mixed n = 4 are certificates accepted by `k4/check4.py`; sensitivity: ignoring (OC₄) gives raw failures. Single implementation: that LB₄ never fails is `k4/lb4.c`'s own count in every class; `check4.py` independently confirms that its outputs are EFX₀, D2 and cover every profile, where stored (not for pure n = 4 and n = 5). 1,139,100,918,624 strict and 24,694,052,160 tied profiles, 0 failures; the hard cases are rare (pure n = 4: a later insertion sequence for 0.006% of profiles, a rotation for 2.8%) |
| K4.LB4.X | Smaller variants of LB₄ fail (smallest failure; `k4/lb4.md` §3): LB⁺'s shape (index insertion, owner r, else one rotation) with need-shrinking upgrades at n = 2, m = 5, and with envy-free upgrades only at n = 3, m = 6; no rotation at n = 3, m = 5; every fixed insertion rule tested (index, block lookahead, least ω, a > b + c first, r leads the last block) with LB₄'s other steps and one rotation at n = 2, m = 5, and index insertion with every upgrade policy and one rotation at n = 3, m = 6; the owner's needs from its base, as at k = 3, at pure n = 4, m = 8; only the last block's leader searched, for every run of Phase 1 at n = 4, m = 8 with three 4-good agents (smallest found), and for the index run at pure n = 4, m = 8 | EVIDENCE | `attempts/lb4-lbplus-shape.md`, `attempts/lb4-no-rotation.md`, `attempts/lb4-fixed-insertion.md`, `attempts/lb4-owner-needs-from-base.md`, `attempts/lb4-last-block-leader.md`, `attempts/lb4_variants.py`, `k4/lb4.c`, `k4/lb4_run.py`, `k4/lb4_brute.py`, `k4/lb4_variant_runs.sh`, `results/k4_lb4_variants.log`, `results/k4_lb4_variants_repro.log`, `results/k4_lb4_i9_run.log`, `results/k4_lb4_i8.log`, `results/k4_lb4_nested_n4.log`, `results/k4_lb4_nested_pure4.log`, `results/k4_lb4_nested_every.log`, `results/k4_lb4_nested_n5.log` | | each smallest failure reproduced by `attempts/lb4_variants.py` (`results/k4_lb4_variants_repro.log`), and brute force finds EFX₀ allocations with at most one large bundle at the failing profile (so the variant fails, not K4.D); every count is in the logs, with its exact options. So with one rotation the insertion order matters at k = 4, unlike LB⁺; with up to three nested rotations and every upgrade policy, index insertion fails nowhere on any certified core (with the single policy it does at n ≤ 3) |
| K4.C4.FRAME | LB₄ʳ (`k4/lb4.md` §5, with LB₄'s steps of §2) is defined in Lean (`EFX.LB4R.Succeeds`: Phase 1(τ), one of the three upgrade policies, at most three rotations, and an output of the owner step) and Theorem C₄ is stated (`EFX.LB4R.TheoremC4`: for every strict profile of every k = 4 core and every insertion sequence τ, LB₄ʳ(τ) succeeds; `EFX.LB4R.TheoremC4index`: τ = index order). C₄∃ (`EFX.LB4R.TheoremC4exists`: every strict profile of every k = 4 core has a completion satisfying (OC₄) of a valid pre-allocation, the owner's needs from its bundle, `EFX.LB4.SoundCompletion`) ⟺ K4.D on strict cores (`EFX.LB4R.C4exists_iff`, the converse by `EFX.LB4R.sound_of_d2`: a D2-shaped EFX₀ allocation is a sound completion). The frame's content is the equivalence and the LB₄ʳ ⟹ C₄∃ direction: C₄ (index order) implies C₄∃, hence that every k = 4 core has an EFX₀ allocation with at most one bundle of more than two goods and that every instance with at most four relevant goods per agent has an EFX₀ allocation; for the latter C₄∃ on connected cores with a 4-good agent suffices (`EFX.LB4R.C4existsConn`) | PROVED | `k4/lb4.md`, `lean/EFX/LB4R.lean` | `EFX.LB4R.target4_of_C4exists`, `EFX.LB4R.k4D_of_C4exists`, `EFX.LB4R.C4exists_iff`, `EFX.LB4R.sound_of_d2`, `EFX.LB4R.conn_of_C4exists`, `EFX.LB4R.target4_of_C4existsConn`, `EFX.LB4R.C4exists_of_C4`, `EFX.LB4R.C4exists_of_C4index`, `EFX.LB4R.target4_of_C4`, `EFX.LB4R.target4_of_C4index`, `EFX.LB4R.k4D_of_C4index`, `EFX.LB4R.sound_of_succeeds`, `EFX.LB4R.Inv.sound`, `EFX.LB4R.phase1State_inv`, `EFX.LB4R.upgrade_inv`, `EFX.LB4R.rotStep_inv`, `EFX.LB4R.output_big_base`, `EFX.LB4R.rotStep_valid`, `EFX.LB4R.theoremC4index_of_C4` | machine-checked in Lean (`formal/k4-c4-frame`, PR #35); C₄, C₄∃ and `C4existsConn` are hypotheses of the theorems, not axioms, and all are open, so K4.D and K4.T keep their status. **C₄ as stated is claimed false**: PR #33 (`k4/c4.md` §7, Proposition H, unreviewed) claims that LB₄ʳ with index insertion fails on the core H_5 (n = 21, m = 53); if that holds, `EFX.LB4R.target4_of_C4`, `EFX.LB4R.target4_of_C4index` and `EFX.LB4R.k4D_of_C4index` have a false hypothesis (the implications stay proved), and C₄∃, being K4.D on strict cores, is untouched by the claim. The definition of LB₄ʳ must be checked against §2 and §5; the choices made where the prose leaves room are listed in the module doc of `lean/EFX/LB4R.lean`, the single source for them. Proved on the way: every reachable state is a valid pre-allocation whose unmarked agents hold exactly their pick (`EFX.LB4R.Inv`: Phase 1, upgrades and rotations keep it), so every output is a sound completion (EFX₀, at most one large bundle, Theorem 1′₄) and a base of three or more goods is the owner's. (V1) and (V2) are required by `RotStep` (checked in `RotChecks`), not derived; `EFX.LB4R.rotStep_valid` reads them off. Non-vacuity (not a ledger item): `lean/EFX/LB4RExamples.lean` has two certified cores on which LB₄ʳ succeeds, one of them after a rotation. K4.D for cores with ties goes through K4.TIE's perturbation |
| K4.GM.CEX | Conjecture GM₄ (row K4.LSP.GM): for every strict profile of every k = 4 core, every junk-free EFX₀ partial allocation that maximizes the level sum Σℓ among all junk-free EFX₀ partial allocations admits a placement of its pool. Equivalently: no dead end maximizes Σℓ | REFUTED | `k4/gm4.md`, `k4/gm4_counterexample.py`, `results/k4_gm4_counterexample.log`, `attempts/k4-gm4-level-sum.md`, `k4/gm4_fast.c`, `k4/gm4_run.py`, `results/k4_gm4_4_mixed_sample.log`, `results/k4_gm4_4_pure_sample.log`, `results/k4_gm4_4_n4_2.log`, `results/k4_gm4_5_pure_sample.log`, `results/k4_gm4_5_n4_4_sample.log`, `results/k4_gm4_ls4plus_gmfail.log` | | Smallest counterexample found (instance E; n = 4, m = 7, two 4-good agents): agents 0: {0:3, 2:10, 4:6, 6:2}, 1: {1:3, 3:4, 5:8, 6:2}, 2: {2:4, 3:2, 6:3}, 3: {4:2, 5:4, 6:3}. The maximum ({0,4}, {1,3}, {2}, {5}) with pool {6} and Σℓ = 18 admits no placement, and no complete EFX₀ allocation weakly dominates it. Four single-agent rebundles reach it from the empty allocation, so LS4⁺_n fails under some valid choices. Mechanism: the Lemma-5 champions of the two sources need the same pool good, and no one-good source exists. Confirmed by an independent plain-Python brute force from the raw definition that shares no code with the search (instances A–F: pure n = 4 and two 4-good agents). EFX₀, the absence of a placement and the rebundle path of E are also checked by hand (`k4/gm4.md` §2.1). The coordinator reports an independent re-check of all counterexamples (not recorded in this repository). `k4/gm4_counterexample.py` also re-checks, for claims 1–4, all 136 distinct maxima without a placement from the random and exhaustive runs (BATCH). Frequency (one implementation, `k4/gm4_fast.c`): 3 of 39,150,000 random n = 4 profiles with one to three 4-good agents; 4 of 43,800,000 pure n = 4; 128 maxima in the exhaustive run with two 4-good agents (724,847,616 profiles); 3 of 93,480,000 pure n = 5; 1 of 19,692,000 n = 5 with four 4-good agents. LS4⁺'s default rule and `-DALT` complete all 136 profiles without a coalition move (`results/k4_gm4_ls4plus_gmfail.log`). K4.LSP.RUN is unaffected, but the rule's correctness cannot be proved through GM₄ or GM₄∃ |
| K4.GM.S | Conjecture GM₄ˢ: every Σℓ-maximum with a nonempty pool admits the empty-bundle dump or a single dump | REFUTED | `k4/gm4.md`, `k4/gm4_counterexample.py`, `results/k4_gm4_counterexample.log`, `attempts/k4-gm4-single-dump.md`, `results/k4_gm4_4_n4_1.log` | | Counterexample (instance S; n = 4, m = 6, one 4-good agent): agents 0: {0:1, 2:6, 3:4, 4:8}, 1: {1:2, 2:3, 5:4}, 2: {1:3, 4:4, 5:2}, 3: {3:3, 4:2, 5:4}. The maximum ({4}, {5}, {1}, {3}) with pool {0, 2} admits only placements that split the pool. Exhaustive over n = 4 with one 4-good agent: 2 of 1,131,363 maxima with a pool, both in this core. None for n ≤ 3 |
| K4.GM.E | Conjecture GM₄∃: every strict profile of every k = 4 core has *some* Σℓ-maximum that admits a placement. Equivalently, the largest Σℓ of a junk-free EFX₀ partial allocation is attained by the valued part of a complete EFX₀ allocation. With K4.TIE and K4.CORE it would have given TARGET₄ as an existence argument | REFUTED | `k4/gm4.md`, `k4/gm4_counterexample.py`, `results/k4_gm4_counterexample.log`, `attempts/k4-gm4-existence.md`, `results/k4_gm4_around1_4.log`, `results/k4_gm4_around2_4.log`, `results/k4_gm4_seeds_4.txt`, `results/k4_gm4_gmall_seeds_4.txt`, `k4/gm4_ls4plus_around.py`, `results/k4_gm4_ls4plus_around2_4.log`, `results/k4_gm4_ls4plus_around2_gmall_4.log`, `results/k4_gm4_distinct.log`, `k4/gm4_distinct.py` | | Counterexample (instance G; pure n = 4, m = 7): agents 0: {0:3, 2:6, 5:2, 6:10}, 1: {1:1, 4:6, 5:8, 6:4}, 2: {2:3, 3:6, 5:8, 6:4}, 3: {3:6, 4:3, 5:4, 6:8}. The unique Σℓ-maximum ({0,2}, {1,4}, {5}, {6}) has Σℓ = 21 and no placement; the complete EFX₀ allocations reach at most Σℓ = 20. So no argument that takes a Σℓ-maximum, with any tie-break, gives TARGET₄. Found around the GM₄ counterexamples: 148 distinct such profiles, all in the core of A and G, among the 2,247,609 distinct profiles (2,260,332 runs) that change two agents' types of the 7 seeds (`results/k4_gm4_distinct.log`). None in the random or exhaustive runs; at n = 4 a counterexample needs at least three 4-good agents (exhaustive runs, one implementation). LS4⁺'s default rule completes all 148, all 2,247,609, and the 16,546,776 distinct profiles (73,654,272 runs) around the 148, without a coalition move |
| K4.GM.RUN | GM₄ and GM₄ˢ in small classes: for every strict profile of every k = 4 core with n ≤ 3, every Σℓ-maximum with a nonempty pool admits the single dump (never an empty bundle). For n = 4 with one 4-good agent, and for all-3-good cores with n = 5, every such maximum admits a placement, by the empty-bundle dump or a single dump except 2 maxima (K4.GM.S) | EVIDENCE | `k4/gm4.md`, `k4/gm4_fast.c`, `k4/gm4_explore.c`, `k4/gm4_run.py`, `results/k4_gm4_2.log`, `results/k4_gm4_3.log`, `results/k4_gm4_4_n4_1.log`, `results/k4_gm4_4_n4_2.log`, `results/k4_gm4_k3cores_5.log` | | Exhaustive, one implementation: n = 2 (189,216 profiles; 236,176 maxima, as in #29's `results/k4_gm_2.log`), n = 3 (299,837,376 profiles, 9,227,950 maxima with a pool), n = 4 with one 4-good agent (7,247,232), all-3-good n = 5 (2,270,592). Also n = 4 with two 4-good agents (724,847,616 profiles): GM₄ fails at 128 maxima, all at m = 7 in 3 cores, but every profile has a placeable maximum (GM₄∃ holds in that class). `gm4_fast.c` (branch and bound) agrees with the full enumeration `gm4_explore.c` on n = 2 and on 438,000 pure n = 4 profiles |
| K4.GM.POT | Conjecture: for the potentials Σ_i 2^{ℓ_i} and leximax (level vectors sorted in decreasing order, compared lexicographically), every strict profile of every k = 4 core has a maximum that admits a placement. Either would prove TARGET₄ with K4.TIE and K4.CORE, as an existence argument. The "every maximum" forms are false (instance P: pure n = 4, m = 7) | CONJECTURE | `k4/gm4.md`, `k4/gm4_fast.c`, `k4/gm4_counterexample.py`, `results/k4_gm4_w2_around2_4.log`, `results/k4_gm4_w2_around2b_4.log`, `results/k4_gm4_w3_around2_4.log`, `results/k4_gm4_w3_around2b_4.log`, `results/k4_gm4_w2_around1c_4.log`, `results/k4_gm4_w3_around1c_4.log`, `results/k4_gm4_w3_around2c_4.log`, `results/k4_gm4_w2_4_pure_sample.log`, `results/k4_gm4_w2_4_mixed_sample.log`, `results/k4_gm4_w2_5_pure_sample.log`, `results/k4_gm4_w3_4_pure_sample.log`, `results/k4_gm4_w3_4_mixed_sample.log`, `results/k4_gm4_w3_5_pure_sample.log`, `attempts/k4-gm4-potentials.md` | | Evidence (one implementation). Targeted searches found no profile without a placeable maximum. Around the 7 GM₄ seeds (2,247,609 distinct profiles): 32 distinct profiles with a bad maximum. Around those 32 (5,428,320 distinct profiles): 384 distinct profiles with a bad maximum. Around those 384, with one agent changed (118,656 distinct profiles) and, for leximax, two (13,696,128 distinct profiles), the bad profiles found are exactly the same 384, so the search closed. In all 7,750,832 distinct profiles for Σ 2^ℓ and 19,652,066 for leximax (`results/k4_gm4_distinct.log`). Random runs (pure n = 4, 43.8M; mixed n = 4, 39.15M; pure n = 5, 9.35M): no maximum without a placement for either potential (`k4/gm4.md` §6) |
| K4.GM.VAR | Related statements: (a) H1, at a Σℓ-maximum every source that envies someone admits the single dump; (b) GM₄∃ with the Σℓ-maxima narrowed by a tie-break (largest Σℓ², leximax or leximin); (c) GM₄∃ for Σℓ² as the potential; (d) GM for Σ 2^ℓ and for leximax (every maximum admits a placement); (e) GM∃ for the fixed-priority potential (the level vector in a fixed agent order, compared lexicographically) for every agent order | REFUTED | `attempts/k4-gm4-envier-source.md`, `attempts/k4-gm4-potentials.md`, `k4/gm4.md`, `k4/gm4_counterexample.py`, `results/k4_gm4_counterexample.log`, `k4/gm4_priority.py`, `results/k4_gm4_priority_seeds_4.log`, `results/k4_gm4_priority_gmall_4.log`, `results/k4_gm4_h1_3_sample.log`, `results/k4_gm4_h1_4_pure_sample.log`, `results/k4_gm4_h1_4_mixed_sample.log`, `k4/gm4_explore.c` | | Counterexamples (smallest found), all replayed by the independent brute force: (a) instance H (n = 4, m = 6, one 4-good agent); (b), (c) instance G (unique maximum); (d) instance P (pure n = 4, m = 7); (e) instance Q (instance E with agent order 2, 3, 0, 1: its unique fixed-priority maximum is E's dead end). H1 held on 67,045 envier sources at n = 3 (random) and failed on 100 of 27,914 (pure n = 4) and 947 of 194,709 (mixed n = 4). For every profile tested, some agent order has a placeable fixed-priority maximum (`results/k4_gm4_priority_*.log`) |
| K4.IND.STEP | Induction on the number j of 4-good agents, the private-good step (`k4/induct.md` §3): (Lemma 2) for a good d valued by agent w only and X′ EFX₀ for I − d, X′ + (d → w) is EFX₀ iff nobody envies w in X′; (Theorem 4) if PS holds for every instance with ≤ j four-good agents (row K4.IND.PS), then every instance with ≤ j + 1 four-good agents in which some 4-good agent has a good nobody else values has an EFX₀ allocation, and a minimal counterexample among them is a connected strict k = 4 core all of whose 4-good agents are Q4; (Proposition 5) a minimal counterexample (I, w*) to PS is connected, has no junk good, w* has no private good, and no other agent is R1/R2-peelable; (Lemma 7) with the target w processed last in Phase 1 and holding its top, w is a valid owner of LB⁺'s pre-allocation, so PS(I, w) holds | CONJECTURE | `k4/induct.md` | | proof/k4-induct. Written proofs, not yet reviewed. Theorem 4 does not close the induction: its hypothesis (PS on 𝒞_j) is stronger than TARGET on 𝒞_j, and PS on 𝒞_4 implies TARGET₄. Positive controls in `results/k4_induct_attempts.log` (Lemma 2's equivalence on two instances, brute force) |
| K4.IND.PS | Conjecture PS₄ (prescribed source): for every instance with ≤ 4 relevant goods per agent and every agent w, some EFX₀ allocation leaves w unenvied (no j with v_j(X_w) > v_j(X_j)) | CONJECTURE | `k4/induct.md` | | proof/k4-induct. Evidence in K4.IND.PSE. With K4.IND.STEP it would remove every P4 and PP4 agent from a minimal counterexample; open already for k ≤ 3. Held in the D2 shape wherever tested |
| K4.IND.PSE | PS holds on: every strict profile of every k = 4 core with n = 2 (378,432 PS(I, w) and 252,288 PS(I − p, w) tests); 14.8 million profiles of the n = 3 k = 4 cores (35 cores exhaustively, the other 16 with 100,000 random profiles each; 44.3 million PS(I, w) and 49.7 million PS(I − p, w) tests); 20 random profiles of each of the 32,586 k = 4 cores with n = 4, 5 (3.2 million PS(I, w) and 1.5 million PS(I − p, w) tests); the chain cores H_1–H_5 of `k4/c4.md` §7 (every agent and every private-good insertion, D2 shape, SAT); 162,000 random general additive instances (n = 3, 4, zeros and ties allowed) | EVIDENCE | `results/k4_induct_ps_k4_n2.log`, `results/k4_induct_ps_k4_n3.log`, `results/k4_induct_ps_k4_n45.log`, `results/k4_induct_ht.log`, `results/k4_induct_ps_general.log` | | proof/k4-induct. `k4/induct_ps.py` (early-exit search of `k4/induct.c`, cross-checked against its full enumeration on 889 random instances) and `k4/induct_sat.py` (independent SAT encoding, re-checked by the raw definition; agrees with brute force on 2,362 tests). Single implementation per row; exhaustive rows are evidence for a conjecture, not certificates |
| K4.IND.LBO | Conjecture LBO: in an instance where every agent other than w values three goods and is balanced and w values at most three, some run of Phase 1 with w processed last, some state of LB's upgrade loop (w not upgraded when top-heavy) and at most one LB⁺ rotation give a valid pre-allocation in which w is a valid owner or holds a free slot; hence PS for such instances, the input Theorem 4 needs at j = 0 | CONJECTURE | `k4/induct.md` | | proof/k4-induct, `k4/induct_lbo.py` (exact Lemma 1 tests; every state checked to be a valid pre-allocation). No miss on every ranking profile of every connected k = 3 core with n ≤ 5, on every profile of the J = I − p of the k = 4 cores with n ≤ 3 whose only 4-good agent is P4, and on samples beyond (`results/k4_induct_lbo_*.log`). Each ingredient is needed, and the run must be chosen (`results/k4_induct_lbo_variants.log`) |
| K4.IND.X | The insertion lemma as posed fails: for every fixed repair bound ρ ≤ 3 some core profile has, for every 4-good w and d ∈ R_w, an EFX₀ allocation of I − d at repair distance > ρ from E(I) (smallest n = 2, 2, 3, 5); deleting w with d (ρ ≤ 2) and dropping w's value of d (ρ ≤ 3) fail too; so does taking X′ extremal for each of 9 potentials | EVIDENCE | `attempts/k4-induct-bounded-repair.md`, `attempts/k4-induct-remove-agent.md`, `attempts/k4-induct-value-drop.md`, `attempts/k4-induct-potentials.md`, `attempts/k4_induct_attempts.py`, `results/k4_induct_attempts.log`, `results/k4_induct_n2.log`, `results/k4_induct_n3.log`, `results/k4_induct_n4.log`, `results/k4_induct_n5.log` | | proof/k4-induct. Found by `k4/induct.c`; every smallest case re-derived by an independent brute force (`k4/induct_bf.py`, no shared code). Could become REFUTED rows after review |
| K4.IND.Q4 | For a 4-good agent with no private good, no rule "take X′ ∈ E(I − d) with the fewest agents envying h, give d to h" works for any (w, d, h) on 128 of 2,200 sampled profiles of the n = 3 cores whose 4-good agents are all Q4 (smallest n = 3, m = 5), nor on 813 of the 4,700 sampled profiles of the n = 4 cores whose only 4-good agent is Q4 (the case j = 0 → 1) | EVIDENCE | `attempts/k4-induct-q4-rules.md`, `results/k4_induct_rules_n3.log`, `results/k4_induct_rules_n4_1.log` | | proof/k4-induct, `k4/induct_rules.py`; smallest case re-derived by brute force (`attempts/k4_induct_attempts.py q4-rules`). This is the gap left by K4.IND.STEP |

## Lessons
- About 3,000 random cores "supported" conjecture A; exhaustive search refuted it in minutes (failures: 1 to 747 of 46,656 profiles per hypergraph, median 22; `results/fail_density_6_10.log`. An earlier sample of three hypergraphs showed 2 to 36).
- Every UNSAT claim that refutes a conjecture gets a second, independently written encoding before it enters this ledger.
- Delegating a range to an external theorem hid the smallest counterexamples. Bundles ≤ 2 already fail at n = 3, and conjecture A at n = 4, in the range m ≤ n + 3 that had been left to Mahara and never searched. Searching that range took 70 s.

## Open items
1. ~~Independent re-implementation of the core enumeration (the trust point of R1): reproduce 15 / 211 / 25 / 37.~~ Done: `src/cores_nauty.py` (nauty genbg) matches `gen_cores` up to isomorphism, `results/enum_crosscheck.log`.
2. ~~Verify the Mahara citation (m ≤ n + 3).~~ Done: read in full (arXiv 2107.09901v2, Theorem 3; general monotone valuations, EFX₀ form), `proofs/citations.md`. R1 and R5 do not depend on it (R5 used it for 7-agent cores with ≤ 10 goods until S2.N7); T3 uses it for every n.
3. ~~Regenerate the n = 7, m = 13 certificates; run m = 12 and 11; then n = 8 at m = 15 and 14~~ Done (R2, R4; logs `results/frontier7_12.log`, `results/frontier7_11.log`, `results/frontier8_15.log`, `results/frontier8_14.log`): measured on 4 CPUs, n = 7: m = 12 in 46 s, m = 11 in 11 min; n = 8: m = 15 in 20 s, m = 14 in 33 min (roughly the first half on shared CPUs; the estimate was ~20 min). ~~n = 8, m = 13 (11,478 cores; SAT estimate ~13 h)~~ Done by construction LB instead (S2.N8, about 6 h on 4 CPUs for m = 13; 7 h for m = 13–16). Not run: n = 8, m = 12 (52,889 cores; about a day for LB, 28 h at the logged 1.9 s per core on shared CPUs or about 20 h uncontended; ~4 days for SAT). ~~Also not run: n = 7, m ≤ 10 (37,488 connected cores)~~ Done by construction LB instead of SAT (S2.N7, about 2 h on 4 CPUs); R5 no longer uses Mahara.
4. ~~Characterize the large bundle (Step 2).~~ Done for n ≤ 6: `proofs/construction.md` (structure, S2.A, construction LB). ~~Attempt D for β = 2 via L11~~ done: D2, `proofs/beta2.md` (collector theorem and Lemma O; no path shortening was needed).
5. ~~Conjecture D for disconnected cores in general~~ Done: Corollary D (`proofs/lb_last_step.md`) does not use connectivity; machine-checked in Lean (D).
6. ~~Read the sources in `proofs/citations.md`, including arXiv 2608.03171 and 2608.30203.~~ Done for seven papers (texts supplied by the repository owner): none implies TARGET or D; two give T3. Viswanathan–Mehta read too: ordinary EFX only (removed good must be worth > 0), so it does not imply TARGET. Still [unverified]: Christodoulou et al. (EC 2023), Chaudhury–Garg–Mehlhorn (JACM 2024), and the leads listed as not read.
7. ~~Conjecture D for β = 3 (m = 2n − 2, plan Step 3.2)~~ Done: D3 (certified; `proofs/beta3.md`). The collector theorem takes any number of collectors (D3.M), which settles every core without Q-agents at every β (D3.0); Q-agents are handled by Q-plans (D3.P), proved by hand for q ≤ 2 (D3.2) and checked on the 394 reduced β = 3 cores (D3.Q, D3.S). D for every β is now proved and machine-checked (D; `proofs/lb_last_step.md`, Corollary D).
8. S2.LB: D does not need it (S2.LB+, proved and machine-checked); what remains of S2.LB is that LB's lookahead never reaches the bad case of Theorem A (S2.R). ~~Lean formalization of Theorems 1′, A, B, C and Corollaries D, T~~ Done (`formal/lbplus`, PR #18).
9. (Moot for D and TARGET, which are proved; kept for the record.) A hand proof of D3.Q for q = 3, 4 (three or four agents without a private good), which would make D3 PROVED. These cases need dump targets (`proofs/beta3.md` §6).
10. (Moot for D and TARGET, which are proved; kept for the record.) Conjecture D for β = 4 (m = 2n − 3) by the same route: D3.P, D3.0 and the shortening lemma hold for every β, so it reduces to Q-plans for the reduced β = 4 cores, which have n ≤ 15 (D3.S). Enumerating all cores up to n = 15 with genbg is out of reach; generate the reduced cores from their kernels (≤ 6 branch vertices, ≤ 9 threads) instead. Whether Q-plans always exist for β ≥ 4 is open.
11. (Moot for D and TARGET, which are proved; kept for the record.) Minimal counterexample (plan Step 3.3, `proofs/min_counterexample.md`): TARGET was open only for β ≥ 4 before D, and a minimal counterexample satisfies β + 3 ≤ n ≤ 5β − 6 (MC4 with T3). Next: β = 4 leaves cores with 8 ≤ n ≤ 14 satisfying MC3 and MC6 (MC6 only where the Q-agent does not value the P-agent's other good); listing them needs a kernel-based generator (genbg lists all cores), and n = 8, m = 13 was not certified (now S2.N8). Reductions at Q-agents and at goods of degree ≥ 3 would be needed for unavoidability; the ones tried fail (`attempts/min-cex-pq-reductions.md`).
12. Multigraph extension (MX.*): prove MX.C (the dump in class T), handle U1 when a good with three or more valuers is the top of some valuers but not others (`attempts/multigraph_u1_mixed_top.md`), and find a replacement for the popular matching on profiles that have none (O1 in `proofs/multigraph_extension.md` §5).
13. Local search (`proofs/local_search.md`, rows LS1–LS6): a second, independent written proof of D and TARGET (Theorem C: two-phase local search with augmented envy cycles and a Hall-type junk placement). Needs an independent review and a re-implementation from the text; formalization candidates: Lemma 1 (partial EFX₀ in a core), Lemma 3 (threat bound), then Claims 1–5.
14. ~~(Optional) State `target` in Lean over any linearly ordered cancellative additive commutative monoid (covers ℝ≥0) by formalizing L12 (`proofs/real_values.md`).~~ Done (`formal/real-values`): `EFX.target_ordered` and `EFX.corollaryD_ordered` over any `EFX.OrderedValue` (`lean/EFX/RealValues.lean`), via `EFX.l12`.
15. k = 4 (rows K4.*, `k4/SCOUT.md`): route LB₄ (`k4/SCOUT.md` §5): implement LB with the k = 4 upgrade and owner rules and run it on every strict profile of every core with n ≤ 3 (needs C: 288³ profiles per core), record its smallest failing configurations, prove its soundness (Theorem 1 analogue), state and test the analogue of S2.K (the large bundle holds private pairs), then redo Theorem A and the rotation. Computation: every core with n ≤ 5 that has a 4-good agent (all-3-good cores: row D), and with n = 6 and exactly one, is certified (K4.R5c, K4.R5d, K4.R5p, K4.R6a; compute/k4-frontier, `k4/frontier/README.md`); next: n = 6 with two 4-good agents (119,283 cores, `results/k4_frontier_counts_6.log`; ≈ 24 CPU-hours with `k4/frontier/search.py --tries=2`, plus checking; not planned: effort goes to LB₄ʳ), then three or more. Constructions can be tested against every certified core and profile with `k4/frontier/tester.py` (C plugin, pipe or oracle) (LB₄: see item 18.)
16. k = 4 minimal counterexample (rows K4.MC*, `k4/MINCEX.md`): TARGET₄ for β ≤ 3 (K4.MC6, certified) and β ≤ 4 (K4.MC7, certified; uses the multigraph theorem for 6 graphical all-P4 cores with n = 6, m = 15). Next: certify those 6 directly (288⁶ profiles each; one ran an hour without finishing; symmetry or a k = 4 proof of the multigraph theorem for cores, as T5 did for k = 3, would do); β = 5 (n ≤ 12 by K4.MC4); two-agent gadgets (a C reducer) for the configurations that persist: excess agents adjacent to one another, and Q4 agents. Certifying the closed-xy reduction (two P4 agents sharing two goods of degree 2: one-agent gadgets reduce 73,548 of 82,944 pair profiles, `attempts/k4-mincex-xy-pairs.md`) would cut the domains of 4 of the 6 uncertified cores
17. k = 4 local search (rows K4.LS.*, `k4/local_search4.md`): the k = 3 scheme with arbitrary choices fails at n = 4, m = 7 (dead end, K4.LS.DEAD). A local-search proof of TARGET₄ needs a choice rule that provably avoids dead ends, or moves that can lower some agent's value (as LB₄'s rotation can). Neither simple rule tried does it (default and `-DALT` both fail on about 1 in 10⁶ pure n = 4 profiles). Also open: review Theorem 1, Lemma 5 and Propositions 3, 4, 7; polynomial-time search for exchange cycles and splits; whether dead ends exist at n = 3 (none in 1,020,000 samples; exhaustive needs about 13 CPU-hours with `k4/ls4_deadend.c`). Positive direction (`k4/ls4plus.md`, rows K4.LSP.*): LS4⁺ adds level-sum-raising coalition re-divisions; it had no failure in any run (K4.LSP.RUN, evidence), and its correctness would follow from conjecture GM₄ (maxima of the level sum admit a placement; equivalently, no dead end maximizes the level sum). GM₄ is refuted (K4.GM.CEX, `k4/gm4.md`), and so is its existence form (K4.GM.E): no argument that takes a maximum of the level sum (with any tie-break) and places its pool proves TARGET₄. LS4⁺_n can fail under some choices; its default rule has no known failure, but its correctness cannot be proved through GM₄ or GM₄∃. Next: a proof that the default rule stops at a placeable state; or the existence form for the convex potentials Σ 2^ℓ or leximax (K4.GM.POT, `k4/gm4.md` §6); or a bounded move set with another potential.
18. LB₄ (`k4/lb4.md`, rows K4.LB4*; follows item 15's LB₄ route): LB₄ never fails on the certified cores, but K4.LB4 is open. Needed for a proof: a Theorem A₄ (count the goods that frozen agents and agents with two-good bases need kept out of the large bundle against the free slots; Lemma 2₄ protects the agents with a pick base only when their own slots take their best junk goods, which competes with those counted goods), a Theorem B₄ for LB₄'s rotation, and a rule or exchange argument for the insertion sequence (with one rotation a fixed order fails, and so does searching only the last block's leader, even from the index order: `attempts/lb4-fixed-insertion.md`, `attempts/lb4-last-block-leader.md`). Index insertion with up to three nested rotations and every upgrade policy (`-i0 -u3 -r3 -w1 -c1`) fails nowhere on any certified core, n ≤ 4 and n = 5 with at most two 4-good agents (`results/k4_lb4_nested_n4.log`, `results/k4_lb4_nested_pure4.log`, `results/k4_lb4_nested_n5.log`), and for every insertion order on n ≤ 3 and on n = 4 with at most three 4-good agents (`results/k4_lb4_nested_every.log`): a candidate Theorem C₄ (every run of Phase 1 works after upgrades and at most three rotations); test it for every order on pure n = 4 and n = 5, and beyond the certified cores. Also: review K4.LB4.SH (K4.LB4.S is machine-checked, `lean/EFX/PreAllocK.lean`); test LB₄ on n = 5 with three or more 4-good agents; the analogue of S2.K (what the large bundle contains)
19. Induction on the number of 4-good agents (rows K4.IND.*, `k4/induct.md`, proof/k4-induct): the insertion lemma as posed fails (K4.IND.X); for a private good the step is exactly "X′ with w unenvied" (K4.IND.STEP, Lemma 2), so the P4 and PP4 agents reduce to PS below (Theorem 4). Next: prove PS for k ≤ 3 instances, e.g. LBO (K4.IND.LBO: LB⁺ with the target last; open part: choosing the run when the target ends Phase 1 without its top, cf. PR #37's Lemma X); find an insertion rule for Q4 agents (K4.IND.Q4); review K4.IND.STEP.
